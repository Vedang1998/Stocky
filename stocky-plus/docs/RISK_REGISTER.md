# Risk Register — Phase 0 / Phase 1 planning

| ID | Severity | Risk | Evidence | Mitigation | Residual | Follow-up |
|---|---|---|---|---|---|---|
| R-001 | P0 | Cross-shop IDOR on child mutations | PO/stocktake/transfer/supplier children historically by bare ID | Shop/parent scoping added on critical routes | Landed-cost service still shop-blind internally; no DB-enforced tenancy yet | Phase 1: shopId + composite FKs + RLS + tests |
| R-002 | P0 | Unsafe stocktake Shopify writes | Serial adjusts; available snapshot; incomplete contract | Kill switch OFF; no complete-on-failure | Logic still not production-ready | Phase 5 rebuild |
| R-003 | P0 | Receiving does not update Shopify inventory | `receivePartialPO` DB-only | Receipt writes gated OFF | Merchants may expect inventory change | Phase 4 ledger |
| R-004 | P0 | Wrong forecast / ABC vs Stocky parity | 30/14, OOS, LT, 90-day ABC, no U | Characterization tests lock behavior | Cannot claim parity | Phase 2 engine |
| R-005 | P0 | Missing privacy compliance processing | Stub webhook only | Topics declared in toml; Phase 1 brief proposes real processors (D-021) | No real redact/export yet | Phase 1 after brief approval; legal review (Q-008) |
| R-006 | P0 | Branding / naming compliance | Stocky++ in README/SETUP/worker logs | Softened Admin billing/dashboard labels | Docs still dirty | Q-001 |
| R-007 | P0 | Invalid GraphQL operations vs 2025-10 | Codegen: inventoryLevel args; inventoryTransferComplete missing | Transfer writes OFF | Enablement would fail at runtime | Fix ops; D-023 before sync foundation |
| R-008 | P1 | Premature Boolean billing | `subscriptionActive`; most routes ungated | Dev activate locked down; Phase 1 defers billing/entitlements | Not commercial-ready | Entitlement service later (not Phase 1 implementation) |
| R-009 | P1 | No AI cost controls while marketing historically claimed AI | Buying Table copy cleaned | No LLM calls exist; AI out of Phase 1 scope | Must not reintroduce claims | Phase 7 + pricing doc |
| R-010 | P1 | Hard caps hide catalog | take 50/250 | Documented | Scale failure | Phase 1–2 pagination; performance envelope in brief |
| R-011 | P1 | Uninstall does not stop jobs / clear data | webhook deletes sessions only | Documented; Phase 1 brief proposes immediate disable + redaction | Orphan jobs until implemented | Phase 1; see also R-031 |
| R-012 | P1 | Shopify CLI `app info` crash | Stack overflow | Recorded as FAIL | Distribution unverified | Manual Partner check (Q-002) |
| R-013 | P2 | npm audit reports ~32 high advisories | `npm install` / `npm ci` audit summary | Deferred — **do not** run `npm audit fix` / `--force` in Phase 0 correction work | Supply-chain risk until separate remediation PR | Separate hardening PR with product/security approval |
| R-014 | P1 | Money via JS Number on Phase 1 monetary fact and ingestion paths; legacy handler cleanup separately scheduled | PO create/receive forms historically use JS Number; Phase 1 will persist order/line/refund amounts that later ledgers depend on | **Approved Phase 1 planning mitigation (not implemented):** every Phase 1 order, order-line, adjustment, cancellation, and refund monetary amount must use exact source representation and exact decimal persistence/computation; currency preserved; no `Number`/`parseFloat`/float arithmetic on money; exact reconciliation to Shopify-reported amounts. No new Phase 1 fact path may inherit or reuse unsafe JavaScript-number money logic. Legacy handler cleanup outside the new fact surface may remain separately scheduled. Planning approval does not resolve this risk. | Rounding and ledger inheritance risk until Phase 1 money rules are implemented and independently verified | Do **not** claim R-014 resolved until implementation and exact-money tests pass independent review |
| R-015 | P1 | `main` may merge without required CI | PR #6 merged while Actions run `30470541851` was red | **API-VERIFIED:** ruleset `Protect main` (id `20012314`, enforcement `active`, target `~DEFAULT_BRANCH` / default `main`). Substantive controls verified by ChatGPT/Codex Rulesets API read (2026-07-30): PRs required; review conversations must be resolved; squash-only merges; required check `Lint, typecheck, test, build, Prisma, GraphQL`; strict/up-to-date status-check policy; force pushes blocked; branch deletion blocked. `bypass_actors` property present and `[]` — **API-VERIFIED** by authenticated Claude Code under GitHub user `Vedang1998` (2026-07-30). Claude’s original PR #8 review environment was rate-limited and did not verify these items at that time | Future settings drift could weaken protection | Periodically re-verify that the ruleset remains active and unchanged |
| R-016 | P3 | GraphQL codegen depends on live Shopify network | `npm run graphql-codegen` fetches Admin schema from shopify.dev | Keep in CI for now; document dependency | Offline / Shopify outage fails CI | Future: vendored or cached schema with update policy (out of this gate) |
| R-017 | P1 | Incomplete lockfile broke clean Linux `npm ci` | Missing `@emnapi/core@2.0.0-alpha.3`, `@emnapi/runtime@2.0.0-alpha.3`, `@emnapi/wasi-threads@2.0.1` | Minimal lockfile repair + pin `npm@11.5.2` | Residual if npm/Node drift | Follow-up PR #7; keep packageManager pinned |
| R-018 | P3 | npm `engines` / packageManager usability friction (Claude F-012) | Exact pin may confuse local tooling | Document pinned versions; do not broaden upgrades in Phase 0 | Local mismatch risk | Future maintenance |
| R-019 | P3 | GitHub Action major-version drift (Claude F-013) | CI uses pinned major actions that will age | Defer action bumps outside correction gate | Supply-chain / breakage risk | Future maintenance |
| R-020 | P3 | npm `shamefully-hoist` project-config warning (Claude F-014) | CI / install noise | Defer hoist policy cleanup | Cosmetic / install-layout risk | Future maintenance |
| R-021 | P3 | Transfer receive sentinel TODO (Claude F-015) | Unsupported complete path uses sentinel id when Shopify id missing | Contained by unsupported-op error + write flag OFF | Clarity debt | Future maintenance when transfer architecture is approved |
| R-022 | P1 | Application-layer tenancy without DB-enforced isolation (Claude F-016) | Route `shop` filters are necessary but not sufficient | Phase 0 keeps app-layer scoping + denial tests. **Approved Phase 1 planning mitigation (mandatory; not implemented):** canonical `Shop` ownership; direct `shopId` on every merchant-owned row; composite tenant foreign keys; forced PostgreSQL RLS; restricted runtime role (non-owner, no `BYPASSRLS`); separate migration role; default denial without tenant context; restricted Session/Shop bootstrap; real PostgreSQL and pooled-connection isolation tests. Composite FKs without RLS do **not** satisfy this gate. Planning approval does not resolve this risk. | Cross-tenant risk if a query omits shop until Phase 1 enforcement merges and is independently verified | **Mandatory Phase 1 P1 gate — do not downgrade. Implementation not started.** |
| R-023 | P3 | Stale PO child-line denial test title (Claude F-017) | Title mentions PO and line while exercising `addLine` only | Deferred test-hygiene rename | Reviewer ambiguity only | Future maintenance — not a Phase 0 blocker |
| R-024 | P1 | Tenant context leakage through connection pooling | Transaction-local RLS/session variables can leak across pooled clients if not cleared | Require transaction-scoped tenant context; pooled-connection leakage tests; deny access when context absent | Residual until tests pass in CI | Phase 1 PR 2–3 |
| R-025 | P1 | Runtime database role accidentally owns tables | Privileged owner role used by web/workers can bypass intended RLS posture | Separate migration-owner vs restricted runtime role; privilege verification tests | Misconfiguration risk | Phase 1 PR 3 |
| R-026 | P1 | Runtime role accidentally receives `BYPASSRLS` | DB role attribute would defeat forced RLS | Explicit no-`BYPASSRLS` requirement; CI privilege assertions | Ops drift risk | Phase 1 PR 3 |
| R-027 | P1 | Bootstrap access becomes a general tenancy bypass | Session/Shop lookup before tenant context is required | Isolate bootstrap module; forbid merchant-domain queries from bootstrap; boundary tests | Bypass expansion risk | Phase 1 PR 2 |
| R-028 | P1 | Unsafe, incomplete, or unresumable tenant backfill | Existing string `shop` data; child rows without shop; interrupted jobs | Nullable ownership first; resumable/idempotent/batched/checkpointed backfill; before/updated/unresolved counts and checksums; quarantine; never guess; non-null only at unresolved zero | Data corruption / wrong ownership | Phase 1 PR 1 |
| R-029 | P1 | Inconsistent existing parent and child ownership / unresolved ownership quarantine | PO-to-supplier and other cross-domain pairs may disagree on shop | Consistency diagnostics; quarantine with table, row identity, ownership evidence, conflicting evidence, parent lineage, source shop values, reason code, detection run, and status; non-empty quarantine blocks non-null, composite constraints, and RLS for the affected domain; never guess; automated repair only with approved deterministic rule; ambiguous rows escalate to product owner; manual repair audited; irrecoverable rows require explicit disposition; silent deletion prohibited; PR 3 requires accepted zero-unresolved report | Schedule pressure to guess ownership | Phase 1 PR 1 quarantine report; PR 3 entry gate |
| R-030 | P1 | Privacy deletion incompleteness | Stub compliance webhooks; caches, exports, queue payloads, storage objects easy to miss | Deletion manifests; idempotent/retryable processors; tests for partial retry and reinstall; legal retention isolation | Residual until legal-reviewed production policy | Phase 1 PR 7; Q-008 |
| R-031 | P1 | Queued jobs continuing after uninstall | Uninstall currently deletes sessions only | Immediate shop disable; cancel or fail-closed queued work; deny new jobs after uninstall | Orphan processing until implemented | Phase 1 PR 4 / PR 7 |
| R-032 | P1 | Webhook replay or reconciliation failure | Webhooks may duplicate, delay, reorder, or miss | Durable inbox; idempotency/correlation; dead letters; replay/repair; reconciliation runs; discrepancies repaired or visible; durable idempotency after queue-retention expiry | Silent data drift if ignored | Phase 1 PR 4–8 |
| R-033 | P1 | Shopify API-version retirement / invalid ops under sync foundation | Current pin `2025-10`; some ops already invalid; near-retirement versions risk rewrite | Validate GraphQL + webhook fixtures against current stable Admin API before first sync implementation merge; record approved version; exclude inventory mutations | Version drift | D-023; before Phase 1 sync PRs |
| R-034 | P1 | Performance failure at approved engineering test envelope | Historical hard caps; N+1 risk; unbounded memory bulk ingest | Envelope: 50k variants, 15 locations, 750k variant-location states, 1M order-line facts, multi-shop concurrency; no silent caps; query-count assertions; indexed plans; p50/p95 evidence | May miss p95 targets | Phase 1 PR 8 |
| R-035 | P1 | Migrations requiring rollback to a non-tenant-aware application | RLS activation is incompatible with pre-Phase-1 app assumptions | Expansion migrations stay compatible with legacy `shop` columns; RLS rollback only to a tenant-aware release; emergency policy disablement needs incident auth + recovery path; backups verified before production migration approval | Wrong rollback target causes outage or exposure | Phase 1 runbooks; PR 1–3 |
| R-036 | P1 | Blocking DDL / prolonged ACCESS EXCLUSIVE locks during constraint and index rollout | Naive `SET NOT NULL` / validating FK on populated tables at Phase 1 envelope | Low-lock expansion: `CREATE INDEX CONCURRENTLY` outside forbidden transactions; FKs/checks added `NOT VALID` then validated; check-constraint path before final NOT NULL; explicit `lock_timeout`/`statement_timeout` with abort-and-retry; runbook lock-level and max hold duration; production monitoring for blocked queries, waits, deadlocks, lag, tx age, errors; fail closed leaving data/app intact | Merchant-visible outage if naive DDL used | Phase 1 PR 3; migration lock tests |
| R-037 | P1 | Mutable tenant keys (`shopId` reassignment) | RLS USING without restrictive WITH CHECK / missing DB immutability | Immutable `shopId` after create; INSERT only current tenant; UPDATE cannot change `shopId`; DB-enforced immutability plus app validation; implementation report names exact mechanism | Cross-tenant transfer from inside authenticated session | Phase 1 PR 3; isolation tests |
| R-038 | P1 | Client-controlled tenant context | Request params/headers/JSON/shop fields could be used as authority | Tenant authority only from server-side verified Shopify auth + canonical Shop; client identifiers untrusted after authorization and still DB-constrained | Fully RLS-enforced system that is still bypassable | Phase 1 PR 2; client-authority denial tests |
| R-039 | P1 | Unvalidated job / queue envelopes establishing tenant authority | Raw queue payload, domain string, external ID, or client-created message | Server-created persisted validated versioned envelope with shopId, source, correlation/causation, schema version, integrity; workers validate Shop before tenant context; fail closed; replay preserves authority and audit lineage | Background cross-tenant leak | Phase 1 PR 2 / PR 4 |
| R-040 | P3 | Planning-record evidence hygiene for corrected-head CI / SHA labels | Claude P2-12 / P2-13 / P3-14 on planning correction re-review | **Closed at planning-record level (2026-07-30):** P2-12 closed by ChatGPT authenticated verification for head `835088d3…` (run `30564344329`); P2-13 addressed by writing that evidence into `PROJECT_STATUS.md`; P3-14 corrected by splitting primary correction commit vs Claude-reviewed PR head in `PLANNING_CORRECTION_IMPLEMENTATION_REPORT.md`. Planning PR #9 later squash-merged as `9fc1025…`. No implementation risk is resolved merely by planning approval or merge. | Status-sync and implementation tips still need their own exact-head CI before merge | Keep attribution accurate; do not claim Claude verified API-blocked CI |

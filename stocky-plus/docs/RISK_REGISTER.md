# Risk Register — Phase 0 / Phase 1 planning

| ID | Severity | Risk | Evidence | Mitigation | Residual | Follow-up |
|---|---|---|---|---|---|---|
| R-001 | P0 | Cross-shop IDOR on child mutations | PO/stocktake/transfer/supplier children historically by bare ID | Shop/parent scoping added on critical routes | Landed-cost service still shop-blind internally; no DB-enforced tenancy yet | Phase 1: shopId + composite FKs + RLS + tests |
| R-002 | P0 | Unsafe stocktake Shopify writes | Serial adjusts; available snapshot; incomplete contract | Kill switch OFF; no complete-on-failure | Logic still not production-ready | Phase 5 rebuild |
| R-003 | P0 | Receiving does not update Shopify inventory | `receivePartialPO` DB-only | Receipt writes gated OFF | Merchants may expect inventory change | Phase 4 ledger |
| R-004 | P0 | Wrong forecast / ABC vs Stocky parity | 30/14, OOS, LT, 90-day ABC, no U | Characterization tests lock behavior | Cannot claim parity | Phase 2 engine |
| R-005 | P0 | Missing privacy compliance processing | Stub webhook only | Topics declared in toml; Phase 1 brief proposes real processors (D-021) | No real redact/export yet | Phase 1 after brief approval; legal review (Q-008) |
| R-006 | P0 | Branding / naming compliance | Stocky++ in README/SETUP/worker logs | Softened Admin billing/dashboard labels | Docs still dirty | Q-001 |
| R-007 | P0 | Invalid GraphQL operations vs 2025-10 | Codegen: inventoryLevel args; inventoryTransferComplete missing | Transfer writes OFF | Enablement would fail at runtime | Fix ops; D-023 before sync foundation |
| R-008 | P1 | Premature Boolean billing | `subscriptionActive`; most routes ungated | Dev activate locked down; Phase 1 defers billing/entitlements | Not commercial-ready | Entitlement service later (not Phase 1 implementation) |
| R-009 | P1 | No AI cost controls while marketing historically claimed AI | Buying Table copy cleaned | No LLM calls exist; AI out of Phase 1 scope | Must not reintroduce claims | Phase 7 + pricing doc |
| R-010 | P1 | Hard caps hide catalog | take 50/250; current `fetchLocations` `first: 50`; thin catalog ingest | PR 5 planning (D-053) proposes complete location pagination and uncapped catalog/JSONL ingest; **not implemented**; not closed by planning approval | Scale failure until implementation and >50 / >250 tests | PR 5 implementation (when authorized); full R-034 envelope remains PR 8 |
| R-011 | P1 | Uninstall does not stop jobs / clear data | webhook deletes sessions only | Documented; Phase 1 brief proposes immediate disable + redaction | Orphan jobs until implemented | Phase 1; see also R-031 |
| R-012 | P1 | Shopify CLI `app info` crash | Stack overflow | Recorded as FAIL | Distribution unverified | Manual Partner check (Q-002) |
| R-013 | P2 | npm audit reports ~32 high advisories | Base main `8ccc8d29…` and PR #11 head both report **32 high / 0 critical** via `npm audit --package-lock-only` (F-F06 investigation 2026-07-31). Count **unchanged** by PR #11. New direct deps `pg` / `@types/pg` introduced no new advisories. | Deferred — **do not** run `npm audit fix` / `--force` in PR #11. Track only; no false claim of resolution. | Supply-chain risk until separate remediation PR | Separate hardening PR with product/security approval; see also R-062 |
| R-014 | P1 | Money via JS Number on Phase 1 monetary fact and ingestion paths; legacy handler cleanup separately scheduled | PO create/receive forms historically use JS Number; Phase 1 will persist order/line/refund amounts that later ledgers depend on | **Approved Phase 1 planning mitigation (not implemented):** every Phase 1 order, order-line, adjustment, cancellation, and refund monetary amount must use exact source representation and exact decimal persistence/computation; currency preserved; no `Number`/`parseFloat`/float arithmetic on money; exact reconciliation to Shopify-reported amounts. No new Phase 1 fact path may inherit or reuse unsafe JavaScript-number money logic. Legacy handler cleanup outside the new fact surface may remain separately scheduled. Planning approval does not resolve this risk. | Rounding and ledger inheritance risk until Phase 1 money rules are implemented and independently verified | Do **not** claim R-014 resolved until implementation and exact-money tests pass independent review |
| R-015 | P1 | `main` may merge without required CI | PR #6 merged while Actions run `30470541851` was red | **API-VERIFIED:** ruleset `Protect main` (id `20012314`, enforcement `active`, target `~DEFAULT_BRANCH` / default `main`). Substantive controls verified by ChatGPT/Codex Rulesets API read (2026-07-30): PRs required; review conversations must be resolved; squash-only merges; required check `Lint, typecheck, test, build, Prisma, GraphQL`; strict/up-to-date status-check policy; force pushes blocked; branch deletion blocked. `bypass_actors` property present and `[]` — **API-VERIFIED** by authenticated Claude Code under GitHub user `Vedang1998` (2026-07-30). Claude’s original PR #8 review environment was rate-limited and did not verify these items at that time | Future settings drift could weaken protection | Periodically re-verify that the ruleset remains active and unchanged |
| R-016 | P3 | GraphQL codegen depends on live Shopify network | `npm run graphql-codegen` fetches Admin schema from shopify.dev | Keep in CI for now; document dependency | Offline / Shopify outage fails CI | Future: vendored or cached schema with update policy (out of this gate) |
| R-017 | P1 | Incomplete lockfile broke clean Linux `npm ci` | Missing `@emnapi/core@2.0.0-alpha.3`, `@emnapi/runtime@2.0.0-alpha.3`, `@emnapi/wasi-threads@2.0.1` | Minimal lockfile repair + pin `npm@11.5.2` | Residual if npm/Node drift | Follow-up PR #7; keep packageManager pinned |
| R-018 | P3 | npm `engines` / packageManager usability friction (Claude F-012) | Exact pin may confuse local tooling | Document pinned versions; do not broaden upgrades in Phase 0 | Local mismatch risk | Future maintenance |
| R-019 | P3 | GitHub Action major-version drift (Claude F-013) | CI uses pinned major actions that will age | Defer action bumps outside correction gate | Supply-chain / breakage risk | Future maintenance |
| R-020 | P3 | npm `shamefully-hoist` project-config warning (Claude F-014) | CI / install noise | Defer hoist policy cleanup | Cosmetic / install-layout risk | Future maintenance |
| R-021 | P3 | Transfer receive sentinel TODO (Claude F-015) | Unsupported complete path uses sentinel id when Shopify id missing | Contained by unsupported-op error + write flag OFF | Clarity debt | Future maintenance when transfer architecture is approved |
| R-022 | P1 | Application-layer tenancy without DB-enforced isolation (Claude F-016) | Route `shop` filters are necessary but not sufficient | Phase 0 keeps app-layer scoping + denial tests. **PR 2 application-layer mitigation is merged** (PR #13; D-034 / D-035). **PR 3 database-enforcement mitigation is merged** (PR #15; D-040 / D-041): canonical `Shop` ownership; direct `shopId` on every merchant-owned row; composite tenant foreign keys; forced PostgreSQL RLS; restricted runtime role (non-owner, no `BYPASSRLS`); separate migration role; default denial without tenant context; restricted Session/Shop bootstrap; real PostgreSQL and pooled-connection isolation tests. Composite FKs without RLS do **not** satisfy this gate. | **CLOSED FOR PHASE 1 REPOSITORY IMPLEMENTATION — independently verified and merged** at accepted implementation `01dbb6f…`, independent report `a51f03…`, merge `deef5d7…` (D-040 / D-041). Production rollout evidence remains separately gated under R-028 / R-029. | Mandatory Phase 1 P1 gate closed for repository implementation; production activation remains separately gated (R-028 / R-029) |
| R-023 | P3 | Stale PO child-line denial test title (Claude F-017) | Title mentions PO and line while exercising `addLine` only | Deferred test-hygiene rename | Reviewer ambiguity only | Future maintenance — not a Phase 0 blocker |
| R-024 | P1 | Tenant context leakage through connection pooling | Transaction-local RLS/session variables can leak across pooled clients if not cleared | **PR 2 application-layer mitigation merged** (PR #13; D-034 / D-035): TenantDb is request/job-scoped application context (no DB session vars yet); concurrent two-Shop pool isolation tests independently verified. **PR 3:** still required for transaction-local RLS session variables and DB-enforced clearance | **CLOSED FOR PHASE 1 REPOSITORY IMPLEMENTATION — independently verified and merged.** Accepted implementation `01dbb6f…`; independent report `a51f03…`; merge `deef5d7…` (D-040 / D-041). Production rollout evidence remains separately gated. | Phase 1 PR 3 — closed for repository implementation; production rollout separately gated |
| R-025 | P1 | Runtime database role accidentally owns tables | Privileged owner role used by web/workers can bypass intended RLS posture | Separate migration-owner vs restricted runtime role; privilege verification tests (`tenant:roles:verify`) | **CLOSED FOR PHASE 1 REPOSITORY IMPLEMENTATION — independently verified and merged.** Accepted implementation `01dbb6f…`; independent report `a51f03…`; merge `deef5d7…` (D-040 / D-041). Production rollout evidence remains separately gated. | Phase 1 PR 3 — closed for repository implementation; production rollout separately gated |
| R-026 | P1 | Runtime role accidentally receives `BYPASSRLS` | DB role attribute would defeat forced RLS | Explicit no-`BYPASSRLS` requirement; CI privilege assertions | **CLOSED FOR PHASE 1 REPOSITORY IMPLEMENTATION — independently verified and merged.** Accepted implementation `01dbb6f…`; independent report `a51f03…`; merge `deef5d7…` (D-040 / D-041). Production rollout evidence remains separately gated. | Phase 1 PR 3 — closed for repository implementation; production rollout separately gated |
| R-027 | P1 | Bootstrap access becomes a general tenancy bypass | Session/Shop lookup before tenant context is required | **PR 2 application-layer mitigation merged** (PR #13; D-034 / D-035): `app/tenant/bootstrap.server.ts` limited to Session/Shop + canonical Shop enumeration; merchant delegates fail closed; architecture audit fails on bootstrap merchant access; independently verified for PR 2 scope. **PR 3:** remaining database enforcement | **CLOSED FOR PHASE 1 REPOSITORY IMPLEMENTATION — independently verified and merged.** Accepted implementation `01dbb6f…`; independent report `a51f03…`; merge `deef5d7…` (D-040 / D-041). Production rollout evidence remains separately gated. | Phase 1 PR 3 — closed for repository implementation; production rollout separately gated |
| R-028 | P1 | Unsafe, incomplete, or unresumable tenant backfill | Existing string `shop` data; child rows without shop; interrupted jobs | **PR 1 backfill tooling merged on main** (authorized head `6e5b024…`; squash `44a24f3…`; independently accepted at `28e7717…`): proposed-ownership dry-run/apply equivalence; atomic issue+detection+checkpoint; diagnostic phases; pinned pg apply lock with backend-PID unlock proof; reopen semantics; preserved beforeCounts; zero-row UPDATE re-read. | Environment-specific execution, operator review, quarantine resolution, recovery, and accepted zero-unresolved evidence are still required before production use | **Open — PR 1 tooling merged on main; operational backfill risk not eliminated by merge alone** |
| R-029 | P1 | Inconsistent existing parent and child ownership / unresolved ownership quarantine | PO-to-supplier and other cross-domain pairs may disagree on shop | **PR 1 quarantine/diagnostics tooling merged on main** (authorized head `6e5b024…`; squash `44a24f3…`; independently accepted at `28e7717…`): cross-domain diagnostics contribute to `blockingIssueCount` and `COMPLETED_WITH_ISSUES`; durable detection history; reopen on re-detection. Actual environment-specific execution, operator review, quarantine resolution, and accepted zero-unresolved evidence remain required; later enforcement remains required. | Schedule pressure to guess ownership; enforcement transition | **Open — PR 1 tooling merged on main; operational / enforcement-transition risk remains** |
| R-041 | P1 | Lost ownership issue evidence across checkpoint resume | Checkpoint advanced while issues held only in memory | Atomic issue + `TenantOwnershipIssueDetection` upsert inside batch transaction; diagnostic phase checkpoints | Accepted PR 1 implementation; future ops still need run evidence | **CLOSED FOR PR 1 — independently verified at `28e7717…`** |
| R-042 | P1 | Misleading zero-unresolved gate ignoring cross-domain issues | Gate keyed only on unresolvedCounts | blockingIssueCount + COMPLETED_WITH_ISSUES + exit 2 | Accepted PR 1 gate semantics; operational zero-unresolved still required later | **CLOSED FOR PR 1 — independently verified at `28e7717…`** |
| R-043 | P1 | Recurring resolved ownership issue not reopening | RESOLVED sticky on re-detect | reopenAt/reopenCount; status OPEN | Accepted PR 1 reopen semantics | **CLOSED FOR PR 1 — independently verified at `28e7717…`** |
| R-044 | P1 | Pooled advisory lock leakage/reentrancy | Prisma pool session locks | Dedicated pg.Client + TENANT_MAINTENANCE_DATABASE_URL; unlock returns backend PID + result | Accepted PR 1 lock design; operators must still use a direct maintenance URL | **CLOSED FOR PR 1 — independently verified at `28e7717…`** |
| R-045 | P1 | Blocking / silently invalid index rollout | Ordinary CREATE INDEX IF NOT EXISTS | D-024 concurrent tooling + verification + real Prisma migrate-diff drift + bounded statement_timeout | Accepted PR 1 index path; production execution still unauthorized | **CLOSED FOR PR 1 — independently verified at `28e7717…`** |
| R-046 | P2 | Shop-domain normalization collision / overlong labels | Non-ASCII toLowerCase; no length bounds | Reject non-ASCII before lowercasing; DNS length bounds | Accepted PR 1 normalization | **CLOSED FOR PR 1 — independently verified at `28e7717…`** |
| R-047 | P1 | Concurrent-index overlap proof invalid (F-N01) | READ COMMITTED holders; settle-after-await; tautology | Deterministic RR holders + `waiting for old snapshots` + settle-in-handler; prior R9 at `fb04345f…` superseded | Accepted PR 1 proof | **CLOSED FOR PR 1 — independently verified at `28e7717…`** |
| R-048 | P1 | Domain discovery outside recorded subject (F-N02) | Unbounded DISTINCT shop | Starting-evidence-bounded discovery + Session evidence boundary | Accepted PR 1 subject-bounded discovery | **CLOSED FOR PR 1 — independently verified at `28e7717…`** |
| R-049 | P1 | Same-ID replacement undetected (F-N03) | ID-only membership checksum | `phase1-tenant-subject-v2` streaming subject digests | Accepted PR 1 subject digests | **CLOSED FOR PR 1 — independently verified at `28e7717…`** |
| R-050 | P2 | Incoherent starting snapshot (F-N04) | Split autocommit reads | One REPEATABLE READ capture; resume fail-closed | Accepted PR 1 coherent snapshot | **CLOSED FOR PR 1 — independently verified at `28e7717…`** |
| R-051 | P3 | Tautological overlap assertion (F-N05) | `buildSettled \|\| true` | Removed | Accepted PR 1 proof cleanup | **CLOSED FOR PR 1 — independently verified at `28e7717…`** |
| R-052 | P3 | Vacuous lock assertion (F-N06) | Empty lock array | Require positive ShareUpdateExclusiveLock | Accepted PR 1 proof cleanup | **CLOSED FOR PR 1 — independently verified at `28e7717…`** |
| R-053 | P2 | Unbounded ID materialization (F-N07) | Full ID arrays | Keyset streaming digest + constrained-heap test | Accepted PR 1 streaming digests | **CLOSED FOR PR 1 — independently verified at `28e7717…`** |
| R-054 | P3 | Review-artifact chain of custody (F-N08) | Review committed with corrections | Correction-review / follow-up / capable-local reports committed as preserved records | Process expectation for future phases | **CLOSED FOR PR 1 — independently verified at `28e7717…`** |
| R-055 | P3 | Drift stderr disclosure (F-N09) | Raw Prisma stderr | Fail-closed allowlisted differences (F-F05) + defence-in-depth redaction | Accepted PR 1 diagnostics | **CLOSED FOR PR 1 — independently verified at `28e7717…`** |
| R-056 | Gate | Independent review environment blocked (F-F00) | Follow-up review labelled unrestricted but lacked Prisma engines / shopify.dev / capable local tooling | Capable-local review on 2026-07-31 executed the required suite successfully | Future reviews must still refuse readiness when tooling is blocked | **CLOSED FOR PR 1 — independently verified at `28e7717…`** |
| R-057 | P1 | Starting snapshot not DB READ ONLY (F-F01/F-F07) | REPEATABLE READ without `SET TRANSACTION READ ONLY` | First-SQL READ ONLY + persisted settings + SQLSTATE 25006 test | Accepted PR 1 snapshot enforcement | **CLOSED FOR PR 1 — independently verified at `28e7717…`** |
| R-058 | P1 | Unbounded/raw domain evidence (F-F02) | Raw domains / unbounded arrays in resumeMetadata | `phase1-evidence-budget-v1` ceilings, digests, redacted samples, fail-closed overflow | Accepted PR 1 evidence budget | **CLOSED FOR PR 1 — independently verified at `28e7717…`** |
| R-059 | P2 | Overlap proof only at old-snapshot wait (F-F03) | Active build/validation phases unproven | Active scan DML proofs + retained old-snapshot-wait | Accepted PR 1 active-phase proof; claim limited to tested phases | **CLOSED FOR PR 1 — independently verified at `28e7717…`** |
| R-060 | P2 | Fixed snapshot timeout (F-F04) | Hard-coded 180s only | Configurable bounded timeout + phase telemetry + safe diagnostics | Accepted PR 1 timeout design | **CLOSED FOR PR 1 — independently verified at `28e7717…`** |
| R-061 | P2 | Drift denylist fail-open (F-F05) | Regex redaction primary | Fail-closed allowlisted differences; no raw stream passthrough | Accepted PR 1 diagnostics | **CLOSED FOR PR 1 — independently verified at `28e7717…`** |
| R-062 | P2 | Dependency advisory posture (F-F06) | Unclear whether PR #11 changed advisory surface | Compared base main vs PR head: **32 high unchanged**; `pg` introduced **no new advisory**; **no** `npm audit fix` / broad upgrades | Pre-existing advisories remain; separate hardening required | **Open — investigated/tracked; not resolved** |
| R-063 | P3 | Misleading read-only comment (F-F07) | Comment claimed read-only without DB enforcement | Corrected with R-057 | Accepted PR 1 comment/guarantee alignment | **CLOSED FOR PR 1 — independently verified at `28e7717…`** |
| R-030 | P1 | Privacy deletion incompleteness | Stub compliance webhooks; caches, exports, queue payloads, storage objects easy to miss | Deletion manifests; idempotent/retryable processors; tests for partial retry and reinstall; legal retention isolation | Residual until legal-reviewed production policy | Phase 1 PR 7; Q-008 |
| R-031 | P1 | Queued jobs continuing after uninstall | Uninstall currently deletes sessions only | Immediate shop disable; cancel or fail-closed queued work; deny new jobs after uninstall | **CLOSED FOR PR 4 REPOSITORY IMPLEMENTATION** (D-052; cumulative review `ca799848…` / blob `c9fca9b2…` at accepted head `eb757119…`). All five cancellable states cancelled; worker + dispatcher re-check `processingEnabled`. Production execution remains unauthorized. | Preserve uninstall-denial regression gates; PR 7 privacy processors remain separate |
| R-032 | P1 | Webhook replay or reconciliation failure | Webhooks may duplicate, delay, reorder, or miss | Durable inbox; idempotency/correlation; dead letters; replay/repair; reconciliation runs; discrepancies repaired or visible; durable idempotency after queue-retention expiry | **CLOSED FOR PR 4 REPOSITORY IMPLEMENTATION** (D-052; cumulative review `ca799848…` / blob `c9fca9b2…` at accepted head `eb757119…`). Durable inbox, digest idempotency, dead letters, and `JobReplay` lineage independently verified. Catalog/order fact reconciliation remains later Phase 1 PRs. Production execution remains unauthorized. | Preserve control-plane regression gates; fact reconciliation remains PR 5–8 |
| R-033 | P1 | Shopify API-version retirement / invalid ops under sync foundation | Current pin `2025-10`; some ops already invalid; near-retirement versions risk rewrite | Validate GraphQL + webhook fixtures against current stable Admin API before first sync implementation merge; record approved version; exclude inventory mutations | **CLOSED FOR PR 4 REPOSITORY IMPLEMENTATION** (D-052; Q-003 / F-PR4-18). Live `2026-07` target/config agreement, live-schema codegen, invalid-document falsification, and webhook API-version handling independently verified at `eb757119…`. Does **not** authorize production deployment, Partner Dashboard validation, production API-health validation, or production writes. | F-CLAUDE-PR4ACC-02 tracks 2025-10 inbound adapter retirement no later than its Shopify window; do **not** remove it in this PR |
| R-034 | P1 | Performance failure at approved engineering test envelope | Historical hard caps; N+1 risk; unbounded memory bulk ingest | Envelope: 50k variants, 15 locations, 750k variant-location states, 1M order-line facts, multi-shop concurrency; no silent caps; query-count assertions; indexed plans; p50/p95 evidence | May miss p95 targets | Phase 1 PR 8 |
| R-035 | P1 | Migrations requiring rollback to a non-tenant-aware application | RLS activation is incompatible with pre-Phase-1 app assumptions | Expansion migrations stay compatible with legacy `shop` columns; RLS rollback only to a tenant-aware release; emergency policy disablement needs incident auth + recovery path; backups verified before production migration approval | Wrong rollback target causes outage or exposure | Phase 1 runbooks; PR 1–3 |
| R-036 | P1 | Blocking DDL / prolonged ACCESS EXCLUSIVE locks during constraint and index rollout | Naive `SET NOT NULL` / validating FK on populated tables at Phase 1 envelope | Low-lock expansion: `CREATE INDEX CONCURRENTLY` outside forbidden transactions; FKs/checks added `NOT VALID` then validated; check-constraint path before final NOT NULL; explicit `lock_timeout`/`statement_timeout` with abort-and-retry; runbook lock-level and max hold duration; production monitoring for blocked queries, waits, deadlocks, lag, tx age, errors; fail closed leaving data/app intact | Merchant-visible outage if naive DDL used | Phase 1 PR 3; migration lock tests |
| R-037 | P1 | Mutable tenant keys (`shopId` reassignment) | RLS USING without restrictive WITH CHECK / missing DB immutability | Immutable `shopId` after create; INSERT only current tenant; UPDATE cannot change `shopId`; DB-enforced immutability plus app validation; implementation report names exact mechanism | Cross-tenant transfer from inside authenticated session | Phase 1 PR 3; isolation tests |
| R-038 | P1 | Client-controlled tenant context | Request params/headers/JSON/shop fields could be used as authority | **PR 2 application-layer mitigation merged** (PR #13; D-034 / D-035): `requireAdminTenant({ request, params })` + bounded recursive client-hint denial (query duplicates, nested JSON/form, multipart, route params). Matching hints never create authority. Independently verified for PR 2 scope. **PR 3:** remaining database enforcement | **OPEN — PR 2 application-layer mitigation merged; database enforcement remains absent until PR 3** | Phase 1 PR 2–3 |
| R-039 | P1 | Unvalidated job / queue envelopes establishing tenant authority | Raw queue payload, domain string, external ID, or client-created message | **PR 2 transport authentication/integrity merged** (PR #13; D-034 / D-035): HMAC-SHA256 signed `tenant-job-envelope-v1`, dedicated `TENANT_JOB_ENVELOPE_SECRET`, source allowlist, issuedAt skew/age, producers accept branded `TenantAuthority` only. Independently verified for PR 2 transport integrity. Version/shape/Shop matching alone is **not** integrity validation. **PR 4:** HMAC v3 + digest/identity binding to the durable row; v1/v2 merchant-write paths fail closed | **CLOSED FOR PR 4 REPOSITORY IMPLEMENTATION** (D-052; cumulative review `ca799848…` / blob `c9fca9b2…` at accepted head `eb757119…`). Production execution remains unauthorized. | Preserve envelope fail-closed regression gates |
| R-064 | P1 | Nullable tenant compatibility regression | Conjunctive `shopId AND shop` scope hides null-`shopId` PR 1 rows; exact legacy equality hides normalizable variants | D-030 scalable scope: `{ shopId }` OR null+distinct raw legacy `shop in (…)`; no owned-row ID materialization; no silent repair | Third-cycle correction implemented — pending independent verification | Phase 1 PR 2 |
| R-065 | P1 | Nested relation cross-tenant disclosure / mutation | Unscoped include/select/_count and nested selector bypasses; whole-query denial on one unprovable relation | Recursive async scoping + model-aware selector resolver + to-one nulling / to-many filtering | Third-cycle correction implemented — pending independent verification; PR 3 RLS remains defense in depth | Phase 1 PR 2–3 |
| R-066 | P1 | Forgeable job envelope | Shape-only envelope allows worker authority from shopId+domain knowledge | HMAC signature + secret strength + timestamp policy + source allowlist + Redis injection tests | Correction implemented — independently verified closed (F-PR2-03); preserve | Phase 1 PR 2 |
| R-067 | P2 | Architecture scanner bypass | Dynamic import + computed/aliased/destructured delegate; issuer local-const/namespace aliases | Compiler-API constant-folding + tainted provenance + issuer alias tracking + exact-file allowlists | Third-cycle correction implemented — pending independent verification (intra-file boundary documented) | Phase 1 PR 2 |
| R-068 | P2 | Client-hint body/node budget DoS / false rejects | JS string length ≠ UTF-8 bytes; business `shop` arrays false-rejected | Actual ArrayBuffer.byteLength ceiling; key-specific shop-hint semantics; multipart cumulative body | Third-cycle correction implemented — pending independent verification | Phase 1 PR 2 |
| R-069 | P3 | Ownership precheck/mutation TOCTOU | Concurrent re-tenant between assert and write | Serializable internal write transactions + bounded retry | Application mitigation implemented — independently verified closed with residual for caller `$transaction`; PR 3 residual remains | Phase 1 PR 2–3 |
| R-070 | P1 | Top-level compound unique selectors break owned workflows | Compound WhereUniqueInput wrapper keys passed into findFirst | Flatten via MODEL_UNIQUE_SELECTORS then tenant-scope; rewrite mutations to `{ id }` | Third-cycle correction implemented — pending independent verification | Phase 1 PR 2 |
| R-071 | P1 | Tenant scope bind-parameter cliff at ≥32,766 owned rows | `{ id: { in: everyOwnedId } }` exceeds PostgreSQL 32,767 bind limit | Scalable predicates; no row-ID materialization; scale tests to 40,000 | Third-cycle correction implemented — pending independent verification | Phase 1 PR 2 |
| R-072 | P2 | Excessive distinct null-ownership legacy representations | Distinct raw legacy `shop` forms materialize into Prisma `in` lists approaching PostgreSQL’s ~32,765 bind ceiling | `phase1-legacy-evidence-v1` bound (default 1024); fail closed `legacy_evidence_overflow`; no near-limit query; no silent partial results; no raw-value disclosure | **CLOSED for PR 2 at `70f4a80…`** — independently verified | Phase 1 PR 2 |
| R-073 | P2 | Tenant-bearing unique selector coercion | Foreign `shop` / `shopId` in compound unique selectors silently rewritten to authenticated tenant, mutating identically keyed own row | Validate tenant-bearing selector components before flatten/coerce; reject with `foreign_selector_tenant`; own case/whitespace variants remain accepted after normalize | **CLOSED for PR 2 at `70f4a80…`** — independently verified | Phase 1 PR 2 |
| R-074 | P2 | SQL/JavaScript legacy normalization divergence | PostgreSQL `btrim()` (spaces only) ≠ ECMAScript `String.prototype.trim()`; tab/newline/NBSP null-owned rows owned by JS but invisible to SQL discovery | Shared `phase1-shop-domain-v1` trim code-point specification; SQL `btrim` uses exact shared set; JS remains final authority; equivalence corpus tests | **CLOSED for PR 2 at `70f4a80…`** — independently verified (SQL remains locale-sensitive bounded superset) | Phase 1 PR 2 |
| R-075 | P1 | Legacy unique-selector duplicate creation | Canonical shop coercion makes null-owned whitespace/case-variant rows unreachable via shop-bearing unique selectors; `upsert` creates a second row (live `after-auth` ShopSettings path) | Set-valued null-compatibility resolution; rewrite mutations to `{ id }`; ambiguity → `ambiguous_legacy_unique_selector`; upsert update-or-create without guessing | **CLOSED for PR 2 at `70f4a80…`** — independently verified; no duplicate creation reproduced | Phase 1 PR 2 |
| R-076 | P3 | Locale-sensitive candidate discovery | PostgreSQL `lower()` is ctype-dependent; Kelvin-sign and similar values may pass SQL candidate filtering but fail JavaScript normalization | Document SQL as bounded superset; JS `phase1-shop-domain-v1` final authority; JS-rejected candidates never authorize; extras count toward overflow budget | **CLOSED for PR 2 at `70f4a80…`** — independently verified; JS remains final authority | Phase 1 PR 2 |
| R-077 | P3 | Legacy evidence configuration parsing | Non-strict `parseInt` accepts `10abc`/`1e9`/`2048.9`; import-time freeze; silent clamp | Strict entire-string base-10 integer parse; range 1..4096; lazy validated singleton; test-only reset; stable configuration error | **CLOSED for PR 2 at `70f4a80…`** — independently verified (lazy-validation residual accepted) | Phase 1 PR 2 |
| R-078 | P3 | Compatibility overflow blast radius | Overflow incorrectly blocks canonical `shopId_id` and `update({id})` for non-null owned rows | Two-stage ID ops; `shopId_id` canonical path; create/foreign rejection without discovery; document operation matrix | **CLOSED for PR 2 at `70f4a80…`** — independently verified against documented matrix | Phase 1 PR 2 |
| R-079 | P3 | Serializable upsert retry exhaustion | Concurrent upserts create no duplicate row and no corruption, but five of eight adversarial concurrent attempts exhausted three immediate retries and surfaced Prisma `P2034`; current retries have no backoff or jitter | Bounded backoff/jitter and a stable application-level `write_conflict_retry_exhausted` (or equivalent) error before production readiness | **OPEN — accepted PR 2 reliability residual** (not a tenant-isolation or data-integrity blocker) | Phase 1 PR 2 → production readiness |
| R-080 | P1 | Verifier blindness to policy/FK/trigger definition drift | Name/count-only verifiers accepted `USING (true)`, wrong same-named FKs, and disabled triggers | Exact catalog definition verify (`pg_policy` / `pg_constraint` / `pg_trigger`) + adversarial drift suites | **CLOSED FOR PHASE 1 REPOSITORY IMPLEMENTATION — independently verified and merged.** Accepted implementation `01dbb6f…`; independent report `a51f03…`; merge `deef5d7…` (D-040 / D-041). Production rollout evidence remains separately gated. | Phase 1 PR 3 — closed for repository implementation; production rollout separately gated |
| R-081 | P1 | Unsafe partial-apply runtime merchant DML without RLS | Apply could grant runtime DML before RLS existed; deadlock left unrestricted cross-tenant reads | Roles prepare without merchant DML; grants only after verified RLS; failSafe revoke invariant | **CLOSED FOR PHASE 1 REPOSITORY IMPLEMENTATION — independently verified and merged.** Accepted implementation `01dbb6f…`; independent report `a51f03…`; merge `deef5d7…` (D-040 / D-041). Production rollout evidence remains separately gated. | Phase 1 PR 3 — closed for repository implementation; production rollout separately gated |
| R-082 | P1 | Unrecoverable partial enforcement | `enforcementAlreadyApplied` + Prisma drift permanently refused resume | Step-aware resume preflight; interruption/resume suite | **CLOSED FOR PHASE 1 REPOSITORY IMPLEMENTATION — independently verified and merged.** Accepted implementation `01dbb6f…`; independent report `a51f03…`; merge `deef5d7…` (D-040 / D-041). Production rollout evidence remains separately gated. | Phase 1 PR 3 — closed for repository implementation; production rollout separately gated |
| R-083 | P1 | Runtime privileged-role membership escalation | `GRANT owner TO runtime` passed verify; `SET ROLE` escalation | Recursive `pg_auth_members` checks; fail closed; optional audited repair | **CLOSED FOR PHASE 1 REPOSITORY IMPLEMENTATION — independently verified and merged.** Accepted implementation `01dbb6f…`; independent report `a51f03…`; merge `deef5d7…` (D-040 / D-041). Production rollout evidence remains separately gated. | Phase 1 PR 3 — closed for repository implementation; production rollout separately gated |
| R-084 | P1 | Semantic credential URL aliasing | Literal URL compare accepted privileged DB via slash/scheme/host/query aliases | Semantic URL normalize + post-connect identity verification | **CLOSED FOR PHASE 1 REPOSITORY IMPLEMENTATION — independently verified and merged.** Accepted implementation `01dbb6f…`; independent report `a51f03…`; merge `deef5d7…` (D-040 / D-041). Production rollout evidence remains separately gated. | Phase 1 PR 3 — closed for repository implementation; production rollout separately gated |
| R-085 | P1 | Populated enforcement deadlock / prolonged blocking | Empty-table timing claims; populated apply deadlocked under concurrent traffic | Deterministic lock order; finite timeouts; bounded retries; populated concurrency evidence (disposable); production evidence uncollected | **CLOSED FOR PR 3 IMPLEMENTATION — populated disposable evidence independently verified.** Accepted implementation `01dbb6f…`; independent report `a51f03…`; merge `deef5d7…` (D-040 / D-041). Production/staging rehearsal evidence remains open under R-028/R-029 and the rollout plan. Do not claim production-scale traffic was tested. | Phase 1 PR 3 — disposable evidence closed; production/staging rehearsal open under R-028/R-029 |
| R-086 | P1 | Runtime connected-identity verification never invoked by application | `assertSafeRuntimeConnectedIdentity` exists but has no app caller; URL compare alone fails when migration URL absent from web/worker env | Shared runtime identity module; verify before merchant query; fail closed; concurrency-safe init | **CLOSED FOR PHASE 1 REPOSITORY IMPLEMENTATION — independently verified and merged.** Accepted implementation `01dbb6f…`; independent report `a51f03…`; merge `deef5d7…` (D-040 / D-041). Production rollout evidence remains separately gated. | Phase 1 PR 3 — closed for repository implementation; production rollout separately gated |
| R-087 | P1 | Unsafe future-object default ACL inheritance | `pg_default_acl` unchecked; `ALTER DEFAULT PRIVILEGES … GRANT ALL TO stocky_runtime` passes all verifiers; new tables get runtime DML without RLS | Inspect `pg_default_acl`; fail closed; explicit repair mode; future-table privilege tests | **CLOSED FOR PHASE 1 REPOSITORY IMPLEMENTATION — independently verified and merged.** Accepted implementation `01dbb6f…`; independent report `a51f03…`; merge `deef5d7…` (D-040 / D-041). Production rollout evidence remains separately gated. | Phase 1 PR 3 — closed for repository implementation; production rollout separately gated |
| R-088 | P2 | Mutating verification commands erase drift evidence | `tenant:roles:verify` executed `REVOKE CREATE … FROM PUBLIC` before checking | Strictly read-only verify/drift/preflight; digest equality tests | **CLOSED FOR PHASE 1 REPOSITORY IMPLEMENTATION — independently verified and merged.** Accepted implementation `01dbb6f…`; independent report `a51f03…`; merge `deef5d7…` (D-040 / D-041). Production rollout evidence remains separately gated. | Phase 1 PR 3 — closed for repository implementation; production rollout separately gated |
| R-089 | P2 | Incomplete privilege-matrix / sequence / default-privilege coverage | Exact-privilege suite covered 2 of required cases; sequences and defaults untested | Complete matrix + sequence + default-ACL suites with stable issue codes | **CLOSED FOR PHASE 1 REPOSITORY IMPLEMENTATION — independently verified and merged.** Accepted implementation `01dbb6f…`; independent report `a51f03…`; merge `deef5d7…` (D-040 / D-041). Production rollout evidence remains separately gated. | Phase 1 PR 3 — closed for repository implementation; production rollout separately gated |
| R-090 | P2 | Weak populated lock / resume-preflight / vacuous fault CI evidence | Apply-step timings as merchant latency; migration-owner traffic; resume silent on dangerous drift; deadlock CI step vacuous | Runtime-role merchant timings + lock sampling; dangerous-drift acknowledgement; dedicated fault suite | **CLOSED FOR PHASE 1 REPOSITORY IMPLEMENTATION — independently verified and merged.** Accepted implementation `01dbb6f…`; independent report `a51f03…`; merge `deef5d7…` (D-040 / D-041). Production rollout evidence remains separately gated. | Phase 1 PR 3 — closed for repository implementation; production rollout separately gated |
| R-091 | P1 | Non-superuser migration-owner incompatibility | Unconditional `ALTER ROLE … NOSUPERUSER/NOBYPASSRLS` fails under mandated non-superuser `CREATEROLE` owner; enforcement never installs; CI was superuser-only | Fail closed on privileged attrs; alter only legal attributes; non-superuser full-lifecycle suite + CI | **CLOSED FOR PHASE 1 REPOSITORY IMPLEMENTATION — independently verified and merged.** Accepted implementation `01dbb6f…`; independent report `a51f03…`; merge `deef5d7…` (D-040 / D-041). Production rollout evidence remains separately gated. | Phase 1 PR 3 — closed for repository implementation; production rollout separately gated |
| R-092 | P2 | Future-function PUBLIC EXECUTE defaults | Absent `pg_default_acl` function row treated as safe while PostgreSQL built-in default grants PUBLIC EXECUTE; preventive REVOKE stores no row | Persistent safe function defaults; effective-ACL verification; probe-function tests; explicit repair | **CLOSED FOR PHASE 1 REPOSITORY IMPLEMENTATION — independently verified and merged.** Accepted implementation `01dbb6f…`; independent report `a51f03…`; merge `deef5d7…` (D-040 / D-041). Production rollout evidence remains separately gated. | Phase 1 PR 3 — closed for repository implementation; production rollout separately gated |
| R-093 | P2 | Inaccurate implementation-head evidence | Live records claimed `046a3b1` as last runtime/test head; actual was `24cc4d8` | Mechanical path-based classification; PR-body live identity; no self-referential SHA pinning | **CLOSED FOR PHASE 1 REPOSITORY IMPLEMENTATION — independently verified and merged.** Accepted implementation `01dbb6f…`; independent report `a51f03…`; merge `deef5d7…` (D-040 / D-041). Production rollout evidence remains separately gated. | Phase 1 PR 3 — closed for repository implementation; production rollout separately gated |
| R-094 | P2 | Merchant-visible DML-denial window during enforcement | Apply revokes runtime DML then restores after RLS; ~22% `42501` during window was counted but unclassified | Structured SQLSTATE classification; assert allowed classes; document cutover/maintenance coordination | **CLOSED FOR PHASE 1 REPOSITORY IMPLEMENTATION — independently verified and merged.** Accepted implementation `01dbb6f…`; independent report `a51f03…`; merge `deef5d7…` (D-040 / D-041). Production rollout evidence remains separately gated. | Phase 1 PR 3 — closed for repository implementation; production rollout separately gated |
| R-095 | P3 | Expected denial assertion can pass vacuously (PR 3 P3-e) | `assertMerchantErrorSummary` does not require `duringWindowExpectedDenial > 0`; observed review runs did contain denials (66/69) | Add explicit positive denial assertion before production enforcement rehearsal | **OPEN — accepted nonblocking PR 3 residual**; current PR 3 implementation remains accepted (D-040); must correct before staging/production enforcement rehearsal | Phase 1 PR 3 accepted residual → rehearsal gate |
| R-096 | P3 | Revocation-window denial coverage is SELECT-only (PR 3 P3-f) | SELECT fails first and aborts the transaction; INSERT/UPDATE/DELETE denial behavior is not separately observed during the revocation window; pre/post success covers all operations | Add per-operation denial probes before production enforcement rehearsal | **OPEN — accepted nonblocking PR 3 residual**; current PR 3 implementation remains accepted (D-040); must correct before staging/production enforcement rehearsal | Phase 1 PR 3 accepted residual → rehearsal gate |
| R-097 | P3 | Dead enforcement evidence guards (PR 3 P3-g) | Trailing merchant-summary loop cannot throw; `composite_key_ambiguous` is unreachable under the current namespace-qualified catalog model | Remove or replace with live assertions in a focused maintenance correction | **OPEN — accepted nonblocking PR 3 residual**; no current safety defect; may be addressed in the same focused maintenance unit as R-095/R-096/R-098 | Phase 1 PR 3 accepted residual → maintenance |
| R-098 | P3 | CI asserts a decoy migration-owner role (PR 3 P3-h) | Visible shell gate checks `stocky_mig_ci`; substantive lifecycle test creates another fixture owner and correctly verifies that fixture | Align the visible CI assertion with the actual test owner before claiming production-like CI evidence for rollout | **OPEN — accepted nonblocking PR 3 residual**; current PR 3 implementation remains accepted (D-040); must correct before using the CI role assertion as rollout evidence | Phase 1 PR 3 accepted residual → rollout-evidence gate |
| R-099 | P1 | DB/Redis dispatch gap or duplicate enqueue | Crash between claim, BullMQ enqueue, and DB ack; concurrent dispatchers; retry reuses BullMQ jobId; retained terminal queue job falsely acked ENQUEUED (NEW-PR4-C01); stranded retry without attempt budget (NEW-PR4-SC08); unreachable paused allowlist (NEW-PR4-SC06) | JobDispatch sequences; distinct queue job IDs; runnable-presence allowlist without paused; atomic ackEnqueued; stranded ENQUEUED recovery with attempt budget; dual-dispatcher tests | **CLOSED FOR PR 4 REPOSITORY IMPLEMENTATION** (D-052; cumulative review `ca799848…` / blob `c9fca9b2…` at accepted head `eb757119…`). Production execution remains unauthorized. | Preserve dispatch-gap regression gates |
| R-100 | P1 | Durable replay authority or lineage forgery | Replay without fresh envelope / forged digest / rewritten original | New job + JobReplay lineage; envelope signature; digest match; disabled-shop deny; original must be DEAD_LETTERED | **CLOSED FOR PR 4 REPOSITORY IMPLEMENTATION** (D-052; cumulative review `ca799848…` / blob `c9fca9b2…` at accepted head `eb757119…`). Production execution remains unauthorized. | Preserve replay-lineage regression gates |
| R-101 | P1 | Uninstall race permits queued merchant access | Worker validated tenant before uninstall commit; DISPATCH_LEASED blocked uninstall | processingEnabled + cancel all non-terminal including RUNNING/DISPATCH_LEASED; RLS processing gate | **CLOSED FOR PR 4 REPOSITORY IMPLEMENTATION** (D-052; cumulative review `ca799848…` / blob `c9fca9b2…` at accepted head `eb757119…`). Production execution remains unauthorized. | Preserve uninstall-race regression gates |
| R-102 | P1 | Global control-plane role can access merchant-domain data | Over-broad grants on control-plane role; no control-plane RLS; SECURITY DEFINER probe owned by superuser migration role (NEW-PR4-C08) | ENABLE+FORCE RLS; role-only policy; exact privilege/policy drift verification; stocky_receipt_probe_owner provision + REVOKE until provision | **CLOSED FOR PR 4 REPOSITORY IMPLEMENTATION** (D-052; cumulative review `ca799848…` / blob `c9fca9b2…` at accepted head `eb757119…`). Direct privilege probes showed zero merchant grants. Production execution remains unauthorized. | Preserve control-plane privilege/RLS regression gates |
| R-103 | P1 | Persisted webhook projection contains unnecessary PII | Raw payload persistence | Topic sanitizers; projection allowlists; privacy tests; projection bounds | **CLOSED FOR PR 4 REPOSITORY IMPLEMENTATION** (D-052; cumulative review `ca799848…` / blob `c9fca9b2…` at accepted head `eb757119…`). Production execution remains unauthorized. | Preserve projection-sanitizer regression gates; PR 7 privacy processors remain separate |
| R-104 | P1 | Stuck leases or concurrent attempts create duplicate application | Lease/attempt races; RUNNING without heartbeat; no DB single-active-attempt invariant; reaper throw on NULL webhookDeliveryId strands RUNNING and aborts batch (NEW-PR4-C02) | Attempt leases/heartbeats; partial unique active attempt; per-attempt isolated reaper; application_outcome_uncertain dead-letter; application receipts | **CLOSED FOR PR 4 REPOSITORY IMPLEMENTATION** (D-052; cumulative review `ca799848…` / blob `c9fca9b2…` at accepted head `eb757119…`). Production execution remains unauthorized. | Preserve lease/attempt regression gates |
| R-105 | P1 | Shopify API-version pin/fixture/schema drift | Pin vs fixtures vs codegen | Target 2026-07; adapters for 2025-10/2026-07; fixture + graphql-codegen evidence | **CLOSED FOR PR 4 REPOSITORY IMPLEMENTATION** (D-052; Q-003 / F-PR4-18). Three-point pin agreement + live codegen independently verified at `eb757119…`. Does **not** authorize production API-health validation or production writes. | F-CLAUDE-PR4ACC-02 tracks 2025-10 inbound adapter retirement; do **not** remove it in this PR |
| R-106 | P1 | Durable idempotency falsely rejects a legitimate distinct event | Over-broad idempotency key; divergent same-ID payloads discarded; missing webhook ID time-keys | shopId+Shopify webhook ID uniqueness; conflict quarantine; null-ID quarantine | **CLOSED FOR PR 4 REPOSITORY IMPLEMENTATION** (D-052; cumulative review `ca799848…` / blob `c9fca9b2…` at accepted head `eb757119…`). Production execution remains unauthorized. | Preserve conflict/null-ID quarantine regression gates |
| R-107 | P1 | Job state-machine transition corruption | Illegal transitions / rewritten history; TOCTOU updates; completeAttemptFail deadLetter bypass could strand RUNNING (NEW-PR4-C06) | Fail-closed transition graph; DB trigger; CAS/FOR UPDATE; append-only attempts; completeAttemptFail always dead-letters | **CLOSED FOR PR 4 REPOSITORY IMPLEMENTATION** (D-052; cumulative review `ca799848…` / blob `c9fca9b2…` at accepted head `eb757119…`). Production execution remains unauthorized. | Preserve state-machine regression gates |
| R-108 | P1 | Dead-letter or replay evidence can be rewritten or lost | Mutable DL / lost replay | One OPEN DL per job; immutable original; auditable JobReplay | **CLOSED FOR PR 4 REPOSITORY IMPLEMENTATION** (D-052; cumulative review `ca799848…` / blob `c9fca9b2…` at accepted head `eb757119…`). Production execution remains unauthorized. | Preserve dead-letter/replay immutability regression gates |
| R-109 | P1 | Duplicate merchant-domain effects after retry/replay | Non-atomic webhook handlers; no SyncApplicationReceipt; missing webhook ID time-key; short acceptance coverage / v1-v2 bypass / 25P02 residual; ALREADY_APPLIED success without post-rollback receipt verify (NEW-PR4-SC01) | Atomic application receipt; ON CONFLICT DO NOTHING RETURNING; post-rollback Repeatable Read receipt verification; shared v2/v3 finalize; execution-mode matrix; fail-closed missing-ID quarantine; expanded exactly-once + envelope fail-closed tests | **CLOSED FOR PR 4 REPOSITORY IMPLEMENTATION** (D-052; cumulative review `ca799848…` / blob `c9fca9b2…` at accepted head `eb757119…`). Production execution remains unauthorized. | Preserve exactly-once regression gates |
| R-110 | P2 | Control-plane inventory scanner blind spots | Hand-maintained inventory list; planted surfaces passed CI | Compiler-API semantic scanner + exact-file exceptions + negative fixtures | **CLOSED FOR PR 4 REPOSITORY IMPLEMENTATION** (D-052; cumulative review `ca799848…` / blob `c9fca9b2…` at accepted head `eb757119…`). Production execution remains unauthorized. | Preserve scanner/negative-fixture regression gates |
| R-111 | P2 | Dispatch plan and per-shop starvation | Seq scan + disk sort at 50k; global ORDER BY allows one-shop backlog monopoly | Eligible-state partial indexes; index-supported claim; per-shop fairness | **CLOSED FOR PR 4 REPOSITORY IMPLEMENTATION** (D-052; cumulative review `ca799848…` / blob `c9fca9b2…` at accepted head `eb757119…`). Production execution remains unauthorized. | Preserve ordered-plan / fairness regression gates |
| R-112 | P3 | Independent-review finding-count and risk-mapping inconsistency | Report declared 4/7/4 vs headings 4/10/6; R-099…R-108 dispositions shifted vs RISK_REGISTER; stale status next-action language (NEW-PR4-C05); migration fixture hygiene (NEW-PR4-C07); chain-of-custody labelling (NEW-PR4-SC07) | D-043 reconciles counts; D-044/D-045 status docs + fixture hygiene; correction uses permanent risk definitions; original + first + second correction-review reports immutable | **CLOSED FOR PR 4 REPOSITORY IMPLEMENTATION** (D-052; cumulative review `ca799848…` / blob `c9fca9b2…` at accepted head `eb757119…`). Records reconcile. | Preserve identity/chain-of-custody labelling discipline |
| R-113 | P2 | Unbounded persisted webhook projection | No size/depth/node/line-item bounds; untyped scalar pass-through | Configurable validated bounds; fail closed with quarantine; scalar type validation | **CLOSED FOR PR 4 REPOSITORY IMPLEMENTATION** (D-052; cumulative review `ca799848…` / blob `c9fca9b2…` at accepted head `eb757119…`). Production execution remains unauthorized. | Preserve projection-bound fail-closed regression gates |
| R-114 | P2 | Source/configuration corruption by redaction tooling | Committed `REDIS_URL ?? "[REDACTED]"` | Require explicit REDIS_URL; fail closed; scan for other corrupted source strings | **CLOSED FOR PR 4 REPOSITORY IMPLEMENTATION** (D-052; cumulative review `ca799848…` / blob `c9fca9b2…` at accepted head `eb757119…`). Placeholder is now a fail-closed guard. | Preserve fail-closed REDIS_URL guard |
| R-115 | P2 | Production-reachable queue-classification bypass | `__setQueueStateClassificationSeamForTests` mutable global (NEW-CLAUDE-D045-01) | Remove seam; pure `classifyQueueState`; test-local `Job.getState` spies only; export/source guards | **CLOSED FOR PR 4 REPOSITORY IMPLEMENTATION** (D-052; cumulative review `ca799848…` / blob `c9fca9b2…` at accepted head `eb757119…`). Seam absent from production source; absence asserted in CI. | Preserve classification-seam absence guards |
| R-116 | P2 | Overstated SC01 v2/v3 worker evidence | Finalizer-only tests labelled v2/v3; no `processWebhookJob` catch execution; isolation option drop (NEW-CLAUDE-D045-02) | Real v2/v3 worker catch matrix; RepeatableRead observation; truthful CI gates | **CLOSED FOR PR 4 REPOSITORY IMPLEMENTATION** (D-052; cumulative review `ca799848…` / blob `c9fca9b2…` at accepted head `eb757119…`). | Preserve real v2/v3 worker-matrix gates |
| R-117 | P3 | Stale D-045 runtime-head identity label | Implementation report “Final runtime/test head = 10a9154…” after later runtime commits (NEW-CLAUDE-D045-03) | Distinguish primary / mechanical-completion / reviewed / review-report identities | **CLOSED FOR PR 4 REPOSITORY IMPLEMENTATION** (D-052; cumulative review `ca799848…` / blob `c9fca9b2…` at accepted head `eb757119…`). Identity records distinguish roles correctly. | Preserve chain-of-custody identity labelling |
| R-118 | P3 | Dead-letter stranded path omits attempt-budget persistence | `nextAttemptCount` computed but not written on `ENQUEUED → FAILED` terminalize (NEW-CLAUDE-D045-04) | Persist `attemptCount = nextAttemptCount` atomically with terminalize | **CLOSED FOR PR 4 REPOSITORY IMPLEMENTATION** (D-052; cumulative review `ca799848…` / blob `c9fca9b2…` at accepted head `eb757119…`). Attempt budget persisted on retry and terminalize paths. | Preserve attempt-budget persistence gates |
| R-119 | P2 | F-PR4-11 performance gate accepts non-ordered claim plans | Bulk insert without `ANALYZE`; regex accepted shop-leading / sorted Index Scans (P2-D046-01). Independently reviewed closed on D-050 head `62f4cff…` (review `2e1fc399…`). | `ANALYZE "DurableJob"` after seed; assert ordered claim plan properties; fixture regressions; sync-integration `-t` zero-pass fail-closed (P3-D046-01) | **CLOSED FOR PR 4 REPOSITORY IMPLEMENTATION** (D-052). Previously CLOSED on D-050 independent evidence. Cumulative review: not reproduced; ordered-plan assertions falsify. Regression gates remain mandatory. Production execution remains unauthorized. | Preserve ordered-plan / zero-pass regression gates |
| R-120 | P2 | Operational fair-claim path full-scans/sorts eligible DurableJob backlog / active-due population | Historical ROW_NUMBER / D-047 Shop Seq Scan / D-048 active-due O(N) (F-D048-03). Independently reviewed closed on D-050 head `62f4cff…` (review `2e1fc399…`). | D-049 `nextDispatchAt` schedule index + EXPLAIN gates including 1k/5k/20k active-due; D-050 split builders preserve schedule path | **CLOSED FOR PR 4 REPOSITORY IMPLEMENTATION** (D-052). Previously CLOSED on D-050 independent evidence. Cumulative review: not reproduced. Regression gates remain mandatory. Production execution remains unauthorized. | Preserve schedule-index / EXPLAIN regression gates |
| R-040 | P3 | Planning-record evidence hygiene for corrected-head CI / SHA labels | Claude P2-12 / P2-13 / P3-14 on planning correction re-review | **Closed at planning-record level (2026-07-30):** P2-12 closed by ChatGPT authenticated verification for head `835088d3…` (run `30564344329`); P2-13 addressed by writing that evidence into `PROJECT_STATUS.md`; P3-14 corrected by splitting primary correction commit vs Claude-reviewed PR head in `PLANNING_CORRECTION_IMPLEMENTATION_REPORT.md`. Planning PR #9 later squash-merged as `9fc1025…`. No implementation risk is resolved merely by planning approval or merge. | Status-sync and implementation tips still need their own exact-head CI before merge | Keep attribution accurate; do not claim Claude verified API-blocked CI |
| R-121 | P2 | DispatchReadyShop readiness false-negative / drift hides eligible work | **MATERIALIZED as F-D048-01** — concurrent same-shop MIN/overwrite races hid due work; reopened/extended by F-CLAUDE-D049-01 same-statement claim/reconcile snapshot deletes. Independently not reproduced on D-050 head `62f4cff…` (1,000/1,000 races, zero permanent false negatives; review `2e1fc399…`). | D-049 monotonic LEAST earliest hint; D-050 split A/B/C/D claim + fresh-snapshot reconcile; statement-level triggers | **CLOSED FOR PR 4 REPOSITORY IMPLEMENTATION** (D-052). Previously CLOSED on D-050 independent evidence (historical MATERIALIZED record preserved). Cumulative review: not reproduced. Regression gates remain mandatory. Production execution remains unauthorized. | Preserve readiness false-negative regression gates |
| R-122 | P3 | Fair-claim range-pair predicate depends on undocumented planner behaviour | `shopId >= x AND shopId <= x` load-bearing vs equality→eligible_* trap (P3-D047-R09) | Retain with equality-regression CI gate; document residual; prefer index redesign later | **ACCEPTED NONBLOCKING RESIDUAL** (D-052; cumulative review `ca799848…` / blob `c9fca9b2…` at accepted head `eb757119…`). Carried forward after PR #20 squash merge `f618103…`. Equality-regression CI gate exists and ordered-plan assertions fail closed if the planner changes. A planner regression degrades performance and is caught, not silently accepted; it cannot corrupt inventory, tenancy, or correctness. Does not threaten PR 4 correctness or acceptance criteria. Production execution remains unauthorized. | Keep equality-regression / ordered-plan gates; optional later index redesign |
| R-123 | P2 | Multi-shop readiness maintain lock-order / deadlock (F-D048-05) | Concurrent multi-shop DurableJob writers deadlock inside readiness upsert. D-051 independent review (blob `d17df590…` at head `938e998…`): deadlock freedom for the current codebase rests on the audited **transaction-shape invariant** (supported writers are single-shop or single-statement multi-shop), not on `stocky.ready_lock_max_shop`. | D-049 removed MIN RMW; D-050 statement-level triggers; D-051 per-shop advisory. `stocky.ready_lock_max_shop` is **defense-in-depth only** (F-CLAUDE-D051-01). No static writer-shape guard (F-CLAUDE-D051-02 / F-CLAUDE-PR4ACC-03). | **ACCEPTED NONBLOCKING RESIDUAL** (D-052). Carried forward after PR #20 squash merge `f618103…`. Current correctness basis is the audited runtime **transaction-shape invariant**. `stocky.ready_lock_max_shop` is **defense-in-depth only** and is bypassable/clearable by `stocky_control_plane`. **F-CLAUDE-D051-01** remains an accepted P3 characterization. **F-CLAUDE-D051-02 / F-CLAUDE-PR4ACC-03** remains future-maintenance risk. **No static writer-shape guard is implemented in this D-052 synchronization.** No current correctness threat at `eb757119…`. Production execution remains unauthorized. | Future-maintenance static writer-shape guard only; do not implement in this PR |
| R-124 | P3 | CI deadlock-timeout harness late rejection-handler flake (F-D048-06) | Intentionally cancellable promise rejection observer attached late. Independently reviewed closed on D-050 head `62f4cff…` (50/50 harness passes; review `2e1fc399…`). F-CLAUDE-D051-03 / F-CLAUDE-PR4ACC-04 (P3 accepted non-blocking) is a **different** pre-existing F-F03 index-build observability/overlap load-sensitivity flake and does **not** reopen this risk. | Attach rejection observer immediately on promise creation | **CLOSED on D-050 independent evidence.** Historical evidence preserved. Regression gates remain mandatory. **Not reopened** by F-CLAUDE-D051-03 / F-CLAUDE-PR4ACC-04. Record F-F03 harness load sensitivity for PR 1 / PR 3 tooling maintenance, not PR 4 runtime correction. | Phase 1 PR 1/PR 3 tooling maintenance (F-F03); do not reopen for PR 4 |
| R-125 | P1 | Claim/reconcile same-statement snapshot permanently deletes readiness (F-CLAUDE-D049-01) | heal_empty / reconcile in same READ COMMITTED statement as shop lock deletes shops that concurrently gained eligible work. Independently reviewed closed on D-050 head `62f4cff…` (review `2e1fc399…`). | D-050 split A scheduler / B candidates / C lease / D fresh-snapshot reconcile; adversarial race tests | **CLOSED FOR PR 4 REPOSITORY IMPLEMENTATION** (D-052). Previously CLOSED on D-050 independent evidence. Cumulative review: not reproduced. Regression gates remain mandatory. Production execution remains unauthorized. | Preserve split-claim/reconcile regression gates |
| R-126 | P1 | Expired-lease recovery / custom GUC platform abort (F-CLAUDE-D049-02) | Single-shop GUC correctness boundary aborts unqualified cross-shop `recoverExpiredDispatchLeases`. Independently reviewed closed on D-050 head `62f4cff…` (review `2e1fc399…`). | Remove GUC; statement-level multi-shop maintain; bounded expired-lease recovery with FOR UPDATE SKIP LOCKED | **CLOSED FOR PR 4 REPOSITORY IMPLEMENTATION** (D-052). Previously CLOSED on D-050 independent evidence. Cumulative review: not reproduced. Regression gates remain mandatory. Production execution remains unauthorized. | Preserve expired-lease recovery regression gates |
| R-127 | P2 | Global readiness advisory lock serializes unrelated merchants (F-CLAUDE-D050-01) | Independent D-050 measurements on `62f4cff…`: control 530→3,258 tps from concurrency 1→100; intake 859→peak 1,689 then 1,229 tps; lease recovery 373→320 tps; recovery p99 ~864.9 ms / max ~1,008 ms at concurrency 100; unrelated intake blocked ~207 / 1,013 / 3,007 ms when another readiness transaction was held 200 / 1,000 / 3,000 ms. Root cause: one `pg_advisory_xact_lock(hashtextextended('stocky_dispatch_ready_shop_maintain', 0))` for all shops. Independently reviewed closed on D-051 head `938e998…` (review blob `d17df590…`): global key removed; cross-shop non-blocking proven; HOL 1,068 ms → 9 ms; intake/recovery scale instead of convoying. Cumulative review at `eb757119…` (blob `c9fca9b2…`): no regression (cross-shop completion under a held lock independently measured). | D-051 per-shop advisory `hashtextextended('stocky_dispatch_ready_shop_maintain:' \|\| shop_id, 0)` in shopId ASC; additive migration `20260812230000_sync_control_plane_d051_readiness_lock_scope`. Deadlock-freedom correctness basis is the transaction-shape invariant; `stocky.ready_lock_max_shop` is defense-in-depth only. | **CLOSED — no regression** (D-052). Previously CLOSED on D-051 independent evidence. F-CLAUDE-D050-01 CLOSED. Historical evidence preserved. Regression gates remain mandatory. | Preserve per-shop lock / HOL regression gates |
| R-128 | P3 | D-050 fairness/admission contract tests contain non-independent expectations (F-CLAUDE-D050-03) | Stale-fairness test derived expected bound from `fairClaimDegradedStaleRepairBoundCycles` under test; anti-reset 500 ms setup overwritten by later 2 s setup before exercise (F-CLAUDE-D050-03A/B). Independently reviewed closed on D-051 head `938e998…` (review blob `d17df590…`): both contracts mutation-proven to detect drift, including behavioural mutation of trigger SQL. Cumulative review at `eb757119…` (blob `c9fca9b2…`): no regression. | Independent expected formula/literals; distinct +500 ms / >1,000 ms / ~1,000 ms cases; approved max delay 1000 ms and fairness floor 1 ms as test literals | **CLOSED — no regression** (D-052). Previously CLOSED on D-051 independent evidence. F-CLAUDE-D050-03A/B CLOSED. Historical evidence preserved. Regression gates remain mandatory. | Preserve independent fairness/anti-reset contract tests |
| R-129 | P1 | Failed bulk / `partialDataUrl` treated as successful full catalog sync | Official 2026-07: failed operations may expose incomplete JSONL at `partialDataUrl`; `url`/`partialDataUrl` expire after seven days. Current main `ingestBulkVariantCache` has no partial-failure watermark rule. | PR 5 brief chooses **(a)**: do not apply `partialDataUrl` to canonical facts; retain metadata for diagnostics; never advance a success watermark. Planning only — not implemented. | **OPEN — PR 5 planning** (D-053). Catalog completeness remains false until implementation + tests. | PR 5 implementation (when authorized); partial-failure tests |
| R-130 | P1 | Recreated products/variants merged by SKU, barcode, or title | PRD 3.2: recreated variants are new identities. Legacy `ShopifyVariantCache` keys only live variant GID and has no tombstone. | Canonical identity = Shopify GID; tombstone deleted GIDs; no unique SKU/barcode indexes. Planning only. | **OPEN — PR 5 planning** (D-053) | PR 5 implementation; deletion/recreation tests |
| R-131 | P1 | Canonical inventory states stay permanently stale | Official inventory-management guide: changes to `committed`, `reserved`, `damaged`, `safety_stock`, and `quality_control` do not trigger webhooks. Official `inventory_levels/update` sample exposes `available` only. Current handler writes webhook `available` into `InventorySnapshot`. | Webhook is a signal; refetch all eight quantity names; periodic `inventory-state-reconcile` with a **configurable freshness target** (60-minute engineering test hypothesis, not a production cadence lock). Bulk/sharded reconcile; no per-item GraphQL polling. Not Phase 1 exit (PR 8 / R-034). Planning only. | **OPEN — PR 5 planning** (D-053) | PR 5 implementation; missed-webhook refetch tests; R-147 cadence cost |
| R-132 | P1 | Missing `unitCost` permission aborts entire catalog sync | Official InventoryItem `unitCost` requires “View product costs” once granular permissions are enabled. Shopify does **not** document the exact bulk-operation failure mode for unauthorized `unitCost`. | Cheap **non-bulk** capability preflight before catalog bulk; with-unitCost vs no-unitCost document; persist permission/availability state; do not burn a full bulk cycle on denial. Still read-only; `FEATURE_COST_SYNC` DEFAULT OFF. Planning only. Linked: independent review area I. | **OPEN — PR 5 planning** (D-053) | PR 5 implementation; preflight + permission partial-failure tests |
| R-133 | P1 | Canonical fact ingest coupled to forecast / ABC / low-stock | Current `handleInventoryUpdate` calls `computeForecast` and may create `LowStockAlert`. R-004 remains known-wrong Phase 2 behavior. | Canonical applicator must not import forecasting; success independent of legacy projection. Isolation is not a Phase 2 formula change. Planning only. | **OPEN — PR 5 planning** (D-053). R-004 remains open for Phase 2. | PR 5 implementation; characterization / negative coupling tests |
| R-134 | P1 | Polling deprecated `currentBulkOperation` binds the wrong operation | Official 2026-07: `currentBulkOperation` is Deprecated; 2026-01+ poll `bulkOperation(id:)`. Up to five concurrent bulk queries per shop. Current `pollBulkOperation` uses `currentBulkOperation`. | Persist BulkOperation GID; lookup only by id. Forbidden in PR 5 documents/helpers. Planning only. | **OPEN — PR 5 planning** (D-053) | PR 5 implementation; codegen / search gate |
| R-135 | P1 | Unbounded JSONL memory / per-row apply | Current `ingestBulkVariantCache` uses `response.text()` + `split` and per-row upsert. Cannot scale toward 50k variants / 750k states. | Stream line-by-line; bounded memory; batched persistence; checkpoints. Planning only. | **OPEN — PR 5 planning** (D-053). Full R-034 envelope remains PR 8. | PR 5 implementation; streaming / write-count tests |
| R-136 | P1 | Silent location cap at `first: 50` | Current `fetchLocations` returns the first page only. | Complete cursor pagination or validated complete bulk; acceptance test **>50 locations**. Planning only. | **OPEN — PR 5 planning** (D-053). R-010 remains open until caps are actually removed. | PR 5 implementation; >50 location test |
| R-137 | P1 | RLS / tenant context weakened to speed bulk ingest | Batch upserts may tempt a privileged or cross-shop write path. Shop-counter and same-txn checkpoint designs would have invited bootstrap/non-RLS writes or runtime DML on `SyncRun` (F-CLAUDE-PR5PLAN-02 / 05). | Do not weaken RLS. Batches remain inside transaction-local tenant context. New tables on PR 1–3 contract. Generation uses platform sequence `stocky_catalog_observation_gen_seq` (not `Shop`); named sequence **USAGE only** — **no SELECT, no UPDATE, no ownership, no schema-wide `ON SEQUENCES`, no PUBLIC, setval denied, explicit NO CYCLE** — **no** table-privilege bypass. Two-phase checkpoint: runtime denied `SyncRun` DML. Preserve R-102. Planning only. Linked: F-CLAUDE-PR5PLAN-02, F-CLAUDE-PR5PLAN-05, F-CLAUDE-PR5PLAN-08. | **OPEN — PR 5 planning** (D-053) | PR 5 implementation; real PostgreSQL cross-shop + sequence-allowlist + SyncRun denial + Races AE/AF/AG |
| R-138 | P0 | Accidental Shopify inventory, product, or cost writes from PR 5 code | Write helpers (`adjustShopifyInventory`, transfer helpers, `inventoryItemUpdate`) remain in the same module family as bulk ingest. Hand-maintained mutation lists miss `inventoryBulkToggleActivation` / `inventoryDeactivate` (F-CLAUDE-PR5PLAN-15; R-110 precedent). | Read-adapter boundary; **deny-by-default** GraphQL-AST/semantic mutation scanner (R-110); explicit forbidden examples include `inventoryBulkToggleActivation` and `inventoryDeactivate`; negative fixture; flags DEFAULT OFF; no feature-flag enablement. Planning only. Linked: F-CLAUDE-PR5PLAN-15. | **OPEN — PR 5 planning** (D-053). Production inventory writes remain UNAPPROVED. | PR 5 implementation; write-scanner negative fixture tests |
| R-139 | P1 | Lossy JS `Number`/`parseFloat` for variant price, compare-at, or unit cost | `ProductVariant.price`/`compareAtPrice` are `Money` strings; `unitCost.amount` is Decimal string. R-014 remains the Phase 1 order/refund gate (PR 6). | Exact string → `NUMERIC`; persist currency; no float arithmetic. Planning only. | **OPEN — PR 5 planning** (D-053). R-014 remains open for PR 6 order/refund facts. | PR 5 implementation; non-binary-safe round-trip tests |
| R-140 | P2 | Expired bulk result URL treated as completed sync | Official: `url` expires 7 days after completion; `partialDataUrl` expires 7 days after failure. Restart after expiry without a new operation would fake success. | If URL expired, start a new bulk operation; do not mark the previous run `SUCCEEDED`. Planning only. | **OPEN — PR 5 planning** (D-053) | PR 5 implementation; expiry recovery test |
| R-141 | P2 | Bulk query exceeds five connections or two nested connection levels | Official 2026-07 restrictions; invalid documents fail at `bulkOperationRunQuery`. Nesting `media`/`metafields` under variants can exceed depth. | Split catalog vs inventory-level bulk; keep `groupObjects=false`; review connection count. Planning only. | **OPEN — PR 5 planning** (D-053) | PR 5 implementation; document review + codegen |
| R-142 | P2 | Legacy `ShopifyVariantCache` / `InventorySnapshot` remain a second authority | Temporary projections are required so old consumers do not silently reinterpret fields. Duplicate authority becomes permanent if cleanup is never scheduled. | New canonical `*Fact` tables; projection after canonical apply; cannot fail/roll back canonical apply; no destructive drops in PR 5. Cleanup is **not** PR 5. Silent stale projection is tracked separately as R-145. | **OPEN — PR 5 planning** (D-053). Cleanup after consumers read canonical facts. | Later focused cleanup PR; not PR 5 |
| R-143 | P1 | Stale full-sync observation overwrites newer incremental canonical **attributes** | PR 4 `catalog-sync` (`stocky-cron`) and webhook jobs (`stocky-webhooks`) interleave for one shop. Last DB writer would apply stale JSONL attributes. | §6.F clock A: Shopify `updatedAt` / per-name `InventoryQuantity.updatedAt`; reject older attributes; `SELECT … FOR UPDATE` per identity. Presence marker still advances on observed GIDs. Planning only. | **OPEN — PR 5 planning** (D-053) | PR 5 implementation; Race A / stale-attribute tests |
| R-144 | P1 | Full-sync omission treated as authoritative absence, including tombstoning a resource with newer existence than the fence | Shopify documents BulkOperation completion but **not** snapshot isolation / point-in-time completeness. A COMPLETED extraction can omit a still-live GID. In-memory GID sets plus webhook overlap. Comparing Shopify `updatedAt`/`createdAt` to `fenceAt` is the wrong clock. | Presence from containment only. Previously-live unseen GIDs are **ABSENCE CANDIDATES**, not tombstones. Confirm with the approved direct query handle before `ABSENT_CONFIRMED_QUERY`. Generation predicate still skips rows with `existenceRequestGen > fenceGeneration`. A candidate must not tombstone while an overlapping direct LIVE existence observation is unresolved. Never nominate/tombstone from partial bulk. Never compare Shopify timestamps to the fence. Circuit breaker in R-154. Planning only. Linked: F-CLAUDE-PR5PLAN-03. | **OPEN — PR 5 planning** (D-053) | PR 5 implementation; Race B / U / W / O / AL tests |
| R-145 | P1 | Silent stale legacy compatibility projection | Canonical apply isolated from projection can succeed while Buying Table / `InventorySnapshot` stay stale with no DataIssue or health signal. Cross-connection `DataIssue`/`SyncHealth` cannot be atomic with merchant facts (F-CLAUDE-PR5PLAN-06). | Merchant-durable `compatibilityProjectionState`; projection failure cannot roll back canonical facts; bounded rebuild; uninstall fail-closed. `DataIssue` / `SyncHealth` are **derived** via a bounded reconciler (R-156). Planning only. Linked: F-CLAUDE-PR5PLAN-06. | **OPEN — PR 5 planning** (D-053) | PR 5 implementation; projection failure/retry + Race Z tests |
| R-146 | P2 | JSONL resume assumes HTTP Range / random access on Shopify result URLs | Official bulk guide documents a signed download URL and 7-day expiry; it does **not** document Range. A byte-seek resume could skip uncommitted lines or fail. Same-transaction checkpoint is unavailable across runtime vs control-plane (F-CLAUDE-PR5PLAN-05). | Line checkpoint is application progress; re-stream from byte 0; skip/no-op only committed ingest batches; **two-phase** checkpoint never ahead of committed facts (merchant commit first; control-plane ack after; runtime denied `SyncRun` DML). Expiry starts a new BulkOperation with a **new** fence generation. Planning only. Linked: F-CLAUDE-PR5PLAN-05. | **OPEN — PR 5 planning** (D-053) | PR 5 implementation; crash-boundary + re-stream tests |
| R-147 | P2 | Inventory reconcile amplification / API and queue cost | Official non-webhook states require refetch. A locked 5–60 minute full-catalog poll would be O(variants × locations) GraphQL and could starve PR 4 fair claim. | Configurable freshness target; bulk/sharded reconcile; coalesce with webhooks; no per-item polling; 60-minute figure is a pre-production test hypothesis; production cadence waits on PR 8 / R-034. Planning only. | **OPEN — PR 5 planning** (D-053) | PR 5 bounded-work tests; PR 8 load evidence |
| R-148 | P1 | Cross-clock comparison of Shopify `updatedAt` to webhook/local `receivedAt` | Collapsing Shopify resource time, webhook arrival, and local tombstone time into one `sourceVersionAt` sequence. Official webhooks are unordered and not guaranteed. Comparing `updatedAt` to `receivedAt` can false-tombstone or block revival. | Distinct clocks A/B/C in §6.F. Never compare Shopify `updatedAt` to `receivedAt`, `existenceObservedAt`, or `fenceAt`. Existence uses observation **intervals** (`existenceRequestGen` / `existenceResponseGen`) vs `fenceGeneration`. Planning only. | **OPEN — PR 5 planning** (D-053) | PR 5 implementation; Race H / J / AI / AJ tests |
| R-149 | P1 | Stale/out-of-order delete/disconnect signal causes false tombstone | Webhook delete/disconnect treated as canonical deletion using arrival time, without an authoritative live check. A delayed signal can tombstone a GID Shopify currently returns as live. Query failure can be mistaken for absence. | §6.F.6: map identity; authoritative existence check; live → do not tombstone + stale-signal `DataIssue`; confirmed absent → tombstone with existence observation, not `receivedAt`; transient failure → retry/`DataIssue`/DEGRADED, not deletion. Planning only. | **OPEN — PR 5 planning** (D-053) | PR 5 implementation; Race H / N tests |
| R-150 | P1 | Stale-attribute no-op suppresses full-sync presence marker | JSONL apply that no-ops older `updatedAt` might skip `lastSeenFullSyncRunId`, so a GID observed in the complete epoch looks absent and the sweep tombstones it. | Presence marker advances for every GID contained in a complete full sync independently of clock-A apply/no-op. Bounded-memory column update. Planning only. | **OPEN — PR 5 planning** (D-053) | PR 5 implementation; Race K tests |
| R-151 | P1 | Nullable Shopify `updatedAt` leaves facts permanently stale or unordered | Official `InventoryQuantity.updatedAt` is nullable. “Both versions missing → never update” never refreshes. Unrestricted last-writer-wins would let a stale null-version bulk overwrite a newer refetch. Concurrent overlapping null-version observations can invert freshness if `responseEndGen` is treated as Shopify order. | §6.F.9 fallback: Shopify `updatedAt` preferred (clock A takes precedence); else app-issued `[attributeRequestGen, attributeResponseGen]` / `[quantityRequestGen, quantityResponseGen]` from `stocky_catalog_observation_gen_seq` using the direct observation interval (start before request, end after usable response); do not claim `attributeResponseGen` alone proves freshness; overlapping different payloads preserve last unambiguous value + DEGRADED + refetch; identical payloads may converge; bulk null-version uses `fenceGeneration`; merchant `attributeFreshnessState=DEGRADED` until a real Shopify timestamp exists; direct refetch must be able to update. Planning only. | **OPEN — PR 5 planning** (D-053) | PR 5 implementation; Race L / M / T / AK tests |

| R-152 | P1 | Generation allocator placement / atomicity / Shop-row convoy | Independent review F-CLAUDE-PR5PLAN-01 / 02 / 08: `Shop.catalogObservationGen` cannot atomically feed `SyncRun.fenceGeneration` across roles/connections; Shop is bootstrap/non-RLS; a per-shop counter convoys all fact application and churns the tenant-root row. | Platform sequence `stocky_catalog_observation_gen_seq`; direct queries allocate `nextval` **before** the Shopify request and again **after** a usable response (observation interval); fence `nextval` in a committed control-plane txn **before** `bulkOperationRunQuery`; never reuse; gaps harmless; no Shop writes; no row lock across Shopify I/O; named sequence **USAGE only** (no SELECT, no UPDATE, no ownership, no PUBLIC, no schema-wide sequence grant, explicit NO CYCLE, `setval` denied). Planning only — not implemented. Linked: F-CLAUDE-PR5PLAN-01, F-CLAUDE-PR5PLAN-02, F-CLAUDE-PR5PLAN-07, F-CLAUDE-PR5PLAN-08. | **OPEN — PR 5 planning** (D-053) | PR 5 implementation; Races P/Q/R/S/T/AD plus AE–AL |
| R-153 | P1 | Cross-role transaction-boundary assumption (checkpoint + facts, or fence + Shop counter, in one txn) | Independent review F-CLAUDE-PR5PLAN-05: `SyncRun` is control-plane with runtime privileges `[]`; merchant facts are runtime/RLS. No transaction spans the two connections. Naming same-txn checkpoint first invites runtime DML on `SyncRun` or control-plane writes of facts (regresses R-102 / R-137). | Two-phase checkpoint only: merchant commit of facts + `ingestBatchId`, then control-plane `jsonlCommittedLineOrdinal`. Checkpoint may lag, never lead. Fence allocation is a control-plane txn that commits before Shopify I/O. Preserve R-102 / R-137. Planning only. Linked: F-CLAUDE-PR5PLAN-01, F-CLAUDE-PR5PLAN-05. | **OPEN — PR 5 planning** (D-053) | PR 5 implementation; Race E / Y |
| R-154 | P1 | Undocumented bulk completeness / mass-tombstone blast radius | Independent review F-CLAUDE-PR5PLAN-03: Shopify publishes no snapshot/completeness guarantee for COMPLETED bulk JSONL. Treating omission as `ABSENT_FULL_SYNC_SWEEP` can silently hole an arbitrary fraction of a catalog. | Candidates only; circuit breaker on absolute count **and** proportion of LIVE rows (configurable; planning hypothesis not product truth); abort all tombstones and DEGRADED if tripped; bounded batched confirmation; no N+1 storm; partial/failed bulk nominates nothing. Planning only. Linked: F-CLAUDE-PR5PLAN-03. | **OPEN — PR 5 planning** (D-053) | PR 5 implementation; Races U/V/W/O |
| R-155 | P2 | Terminal identity revival from one transient LIVE response | Independent review F-CLAUDE-PR5PLAN-10: official `productDelete` is irreversible, yet unbounded single-observation revival would reattach state to a dead GID. F-CLAUDE-PR5C4-02: two overlapping LIVE responses must not satisfy the two-confirmation threshold. | Terminal GIDs (Product, ProductVariant, InventoryItem, deleted Location) require two **non-overlapping** independent LIVE confirmations (`second.observationRequestGen > first.observationResponseGen`) and `createdAt` match where available; one LIVE opens `TERMINAL_IDENTITY_REVIVAL_CONFLICT` and keeps the tombstone; two overlapping LIVE responses do not revive. InventoryLevel pairs remain reconnectable and exempt. Overlapping LIVE/ABSENT must never bypass these safeguards. Planning only. Linked: F-CLAUDE-PR5PLAN-10, F-CLAUDE-PR5C4-02. | **OPEN — PR 5 planning** (D-053) | PR 5 implementation; Race AB / AJ |
| R-156 | P2 | Diagnostic projection lag (`DataIssue` / `SyncHealth` not atomic with merchant facts) | Independent review F-CLAUDE-PR5PLAN-06: `DataIssue` and `SyncHealth` are control-plane-only. A crash between fact commit and diagnostic write produces the silent state the brief forbids. | Merchant-durable diagnostic columns / outbox; PR 4 `JobAttempt` when no merchant txn ran; bounded idempotent reconciler recreates missing issues and closes orphans; must not change canonical truth; must not report false HEALTHY after reconciliation. Planning only. Linked: F-CLAUDE-PR5PLAN-06. | **OPEN — PR 5 planning** (D-053) | PR 5 implementation; Race Z / F |
| R-157 | P1 | Sequence UPDATE / `setval` privilege allows generation reset and reuse | Official PostgreSQL 18: `nextval` requires USAGE or UPDATE; `setval` requires UPDATE; SELECT is not needed for `nextval`. Granting UPDATE (or USAGE, SELECT, UPDATE) to `stocky_runtime` / `stocky_control_plane` would let application roles reset `stocky_catalog_observation_gen_seq` and reuse observation generations, contradicting the never-reuse invariant. CYCLE would also reuse values after wrap. | Least privilege: migration/schema owner only; both application roles **USAGE only**; no SELECT; no UPDATE; no ownership; no PUBLIC; no schema-wide `GRANT … ON SEQUENCES`; explicit **NO CYCLE**; application roles must fail `setval()`. Keep `SELECT nextval('stocky_catalog_observation_gen_seq')` as the allocation primitive. PR5-F1 created the sequence and USAGE-only privilege checks; this does **not** close the risk. Do **not** close this risk merely because the foundation primitive exists. | **OPEN — PR 5 implementation** (D-054 EFFECTIVE; PR5-F1 sequence/privilege primitive landed; remain OPEN until all allocation paths and regressions are verified) | PR 5 implementation; Races AE / AF / AG / AD |
| R-158 | P1 | Concurrent-response scheduling inversion causes false observation order | Allocating a single generation immediately after Shopify response completion does **not** prove observation-completion order across concurrent workers. Example: response A arrives first; A is descheduled before `nextval`; B’s response arrives second and obtains G1; A resumes and obtains G2. G2 > G1 does not prove A’s Shopify observation was later. Using `responseEndGen` alone can false-tombstone or false-revive. | Direct queries use app-issued observation intervals `[observationRequestGen, observationResponseGen]` (start before request, end after usable response, then identity lock). Generations order **app request lifecycle only**, not Shopify mutation/snapshot time. Overlapping conflicting existence: preserve last unambiguous state, persist `CONCURRENT_EXISTENCE_OBSERVATION_CONFLICT`, refetch after both complete. No network lock. PR5-F1 did not implement the canonical apply engine. Do **not** close this risk merely because the planning mitigation exists. | **OPEN — PR 5 implementation** (D-054 EFFECTIVE; canonical apply engine not yet implemented) | PR 5 implementation; Races AH / AI / AJ / AK / AL |
| R-159 | P2 | Orphaned/expired direct-observation evidence or an unfenced late worker can permanently block an identity or apply stale evidence | Independent review F-CLAUDE-PR5C4-01: in-flight start evidence committed before Shopify I/O had no lease, logical-expiry rule, or late-worker fence. A hard-crashed worker could leave `[requestGen, +∞)` unresolved forever (identity freeze) or, if expiry were physical-cleanup-only, a late original worker could still apply a stale Shopify response after abandonment. Independent review F-CLAUDE-PR5C5-01 / 02 / 03 / 04: unspecified application-node clocks, missing-row fence gap, and missing multi-blocker / apply-after-expiry races can re-enter the same damage class. After Correction 6, ChatGPT identified a remaining stale-token revalidation path: an expired observation may remain lifecycle-state ACTIVE; if a successor relies on expiry and PostgreSQL wall time later moves backward, `clock_timestamp() < leaseExpiresAt` could become true again. Independent review F-CLAUDE-PR5C7-02: persisting `observationResponseGen` while `ACTIVE` created a row class that could neither block nor be durably abandoned, so CASE 2 rollback fencing was unavailable for response-bearing rows. | Finite `leaseExpiresAt` (configured direct Shopify request timeout + finite bounded recovery margin; validated finite maximum; test-configurable short values). Lease invalidity at `clock_timestamp() >= leaseExpiresAt` denies the original apply even before physical rewrite. ACTIVE unexpired resultless observations block overlapping existence mutation but not request issuance; held responses are not replayed as fresh. Tenant-transaction fencing of observation token + ACTIVE + unexpired lease. No `stocky_control_plane` DML on merchant in-flight/fact tables. Bounded PR 4 retry/refetch. **Correction 6:** PostgreSQL `clock_timestamp()` is authoritative for creation and expiry; absolute deadline computed in DB; application-node clocks prohibited for lease correctness; exact valid `<` / expired `>=` boundary (equality is expired); final post-lock fact fence rechecks DB time; missing-row fail-closed; all-active-blocker predicate; Races AM / AO / AP / AQ / AR. **Correction 7:** when expiry is relied upon to unblock a successor canonical mutation, exact expired ACTIVE resultless rows are durably transitioned `ACTIVE -> ABANDONED` inside the same tenant/identity transaction; `ABANDONED` is irreversible; database-clock rollback cannot revalidate a persistently abandoned token; successor mutation and abandonment fencing share that tenant transaction (rollback undoes both); no cross-role transaction; physical DELETE/reaping remains maintenance; Race AS; Race AQ extension. **Correction 8 (F-CLAUDE-PR5C7-02):** `responseGen` is allocated after a usable response but kept in-process; `ACTIVE` ⇒ persisted `responseGen` NULL; `responseGen` is persisted only atomically when the lifecycle leaves `ACTIVE`; planned DB constraint; Race AU. PR5-F1 landed observation schema/constraints; abandonment and apply writers remain outstanding. Do **not** close this risk merely because the planning mitigation exists. Linked: F-CLAUDE-PR5C4-01, F-CLAUDE-PR5C5-01, F-CLAUDE-PR5C5-02, F-CLAUDE-PR5C5-03, F-CLAUDE-PR5C5-04, F-CLAUDE-PR5C7-02. | **OPEN — PR 5 implementation** (D-054 EFFECTIVE; observation schema exists; downstream abandonment/apply writers pending) | PR 5 implementation; Races AM / AN / AO / AP / AQ / AR / AS / AU |
| R-160 | P1 | Canonical fact first-insert path lacks a serialization anchor before the canonical row exists | Independent review F-CLAUDE-PR5C7-01: the sole stated serialization mechanism was `SELECT … FOR UPDATE` on the canonical fact row. Official PostgreSQL 18 documents row-level locks as applying only to retrieved rows; a nonexistent row cannot be locked, and predicate locking is not available under the brief’s READ COMMITTED sweep. Initial sync is required to overlap webhook processing, so concurrent first application of a brand-new identity is a routine path. A uniqueness constraint raises only after independent apply decisions; prescribed `ON CONFLICT DO UPDATE` would silently overwrite rather than re-evaluate interval / blocker / conflict rules. Independent review F-CLAUDE-PR5C8-02: `shopId` preimage encoding was unpinned and no known-answer vectors existed, so independently implemented writers could derive different keys and under-serialize. | Universal exclusive transaction-scoped `pg_advisory_xact_lock` canonical-identity anchor; encoding version `stocky-pr5-canonical-lock-v1`; each UTF-8 component is `<decimal UTF-8 byte length>:<UTF-8 bytes>` with no extra separator; byte length (not JS string length) is authoritative; no trimming, lowercasing, Unicode normalization, or Shopify-domain normalization; `shopId` is the exact stored Prisma `Shop.id` cuid string bytes; resource kinds are exact literals `Product`, `ProductVariant`, `InventoryItem`, `Location`, `InventoryLevel`; SHA-256 digest bytes 0..7 as signed 32-bit big-endian `key1`/`key2`; never a JavaScript `Number`; one canonical key-derivation function; known-answer vectors 1–3 pinned; applies to existing and nonexistent facts, tombstones, first insert, InventoryLevel pairs, and all canonical writers; transaction-scoped only (`pg_advisory_lock` session-level forbidden); no network lock; deterministic multi-identity lock order (ascending `(key1, key2)`, deduplicated); no blind `ON CONFLICT DO UPDATE` correctness path; Race AT; Race AV. PR5-F1 landed the lock derivation/anchor primitive; this does **not** close the risk until every canonical writer uses it. Linked: F-CLAUDE-PR5C7-01, F-CLAUDE-PR5C7-03, F-CLAUDE-PR5C8-02. | **OPEN — PR 5 implementation** (D-054 EFFECTIVE; lock primitive exists; must prove every canonical writer uses it) | PR 5 implementation; Races AT / AV; known-answer vectors 1–4 |
| R-161 | P2 | PR5 advisory identity-lock capacity can exhaust PostgreSQL shared lock-table resources under large multi-identity transactions and concurrency | Independent review F-CLAUDE-PR5C8-01: Correction 8 made one transaction-scoped advisory lock per canonical identity mandatory, while JSONL application contemplated up to 500 identities per transaction. Official PostgreSQL 18: the shared lock table is sized on an average basis as `max_locks_per_transaction` objects per server process or prepared transaction; individual transactions may exceed that average if shared capacity remains; advisory locks share that table with other lockable objects; `max_locks_per_transaction` can only be set at server start. Default 64 is **not** a hard per-transaction cap of 64 locks. | Initial 32-identity canonical-transaction cap (engineering hypothesis; configurable downward; increase requires capacity evidence). Read/verify `max_locks_per_transaction`, `max_connections`, and `max_prepared_transactions`. Planning estimate `shared_lock_object_budget = max_locks_per_transaction * (max_connections + max_prepared_transactions)` is not a guarantee that every slot is available to PR 5. Accept a multi-identity batch only when both `canonicalLocksPerTransaction <= floor(max_locks_per_transaction / 2)` and `canonicalLocksPerTransaction * configuredWorstCaseConcurrentCanonicalTransactions <= floor(shared_lock_object_budget * 0.25)` (25% estimated PR 5 global lock-object budget). Expected deployment concurrency hypothesis: 4 concurrent canonical multi-identity fact transactions, plus one-identity observations in capacity evidence. Automatic downward batch reduction when 32 is unsafe. Never increase PostgreSQL settings automatically; never alter `max_locks_per_transaction` at runtime; never require a database restart from application code. Fail-closed bounded retry: abort; no canonical commit; no half-applied abandonment; release transaction advisory locks through rollback; reduce sub-batch; never split one identity; never unanchored fallback; stop at one identity; then degrade/fail. Mandatory Race AW. PR5-F1 landed the capacity evaluator; arithmetic alone is not production proof. Do **not** close this risk until runtime/deployment/concurrency evidence exists. Linked: F-CLAUDE-PR5C8-01. | **OPEN — PR 5 implementation** (D-054 EFFECTIVE; capacity evaluator exists; deployment/concurrency capacity evidence still required) | PR 5 implementation; Race AW |
| R-162 | P3 | PR5 lock-capacity evaluator accepts unsafe/non-safe integer direct inputs | Independent correction re-review NEW-CLAUDE-PR5F1C-01: `evaluateCanonicalLockCapacity` / `requireIntAtLeast` checks `Number.isInteger` but not `Number.isSafeInteger`, so direct inputs such as `2^53`, `2^53+2`, and `Number.MAX_VALUE` can be accepted and emit precision-loss or `Infinity` diagnostics. Merchant impact is none today: the only production path is `readPostgresLockCapacitySettings`, which already rejects unsafe integers, and real PostgreSQL cannot report such values. | Fail-closed guard on untrusted/direct evaluator inputs (`Number.isSafeInteger` plus an explicit upper bound for derived budgets) before any downstream worker relies on directly configured evaluator inputs. Do **not** reopen PR5-F1. Do **not** treat the PostgreSQL settings-reader coverage as sufficient for a future direct-input worker. Linked: NEW-CLAUDE-PR5F1C-01. | **OPEN — PR 5 downstream** (must resolve before a worker relies on untrusted/directly configured evaluator inputs) | Downstream lock-capacity consumer; `lock-capacity.test.ts` unsafe-integer / `MAX_VALUE` cases |
| R-163 | P3 | PR5 no-Shopify / mutation-safety scanner is non-recursive and can miss nested production modules | Independent correction re-review NEW-CLAUDE-PR5F1C-02: `foundation-safety.test.ts` uses non-recursive `readdirSync`. A nested production module was proven unscanned. Merchant impact is none today — no subdirectory exists. Future PR5 lanes that add structure can erode the defense-in-depth guard. | Downstream admin-read/query-boundary lane must make production module enumeration recursive and mutation safety semantic/deny-by-default as required by the PR5 brief. Do **not** reopen PR5-F1. Linked: NEW-CLAUDE-PR5F1C-02. | **OPEN — PR 5 downstream** (admin-read/query-boundary lane) | Recursive module discovery; semantic deny-by-default mutation scan |
| R-164 | P3 | Canonical fact physical DELETE remains technically reachable despite the tombstone-only product contract | Independent correction re-review NEW-CLAUDE-PR5F1C-04: nested `delete` / `deleteMany` remain on canonical relations; `stocky_runtime` holds `DELETE` on the new fact tables; the immutability trigger guards `shopId` only; no trigger forbids `DELETE`. The PR5 product contract requires canonical Shopify deletion to tombstone and preserve historical identity. Merchant impact is none today — foundation-only, no writer, no production, flags OFF. Tenant scoping is intact (cross-shop delete affects 0 rows). | Mandatory acceptance gate for the canonical applicator lane: all normal PR5 canonical writer/apply paths must mechanically use tombstone / existence-state transitions and must not physically delete canonical fact rows. Any physical-delete capability retained for future maintenance must be outside ordinary canonical apply APIs, explicitly bounded, tenant-safe, and separately authorized. Do **not** weaken tenant/RLS architecture to solve it. Do **not** close R-164 in this docs PR. Do **not** reopen PR5-F1. Linked: NEW-CLAUDE-PR5F1C-04. | **OPEN — PR 5 applicator lane** (tombstone-only runtime contract not mechanically enforced against physical delete) | Canonical applicator lane; refuse physical delete on ordinary apply APIs |

// Negative fixture — worker constructing PrismaClient.
import { PrismaClient } from "@prisma/client";

const prisma = new PrismaClient();

export async function badWorker() {
  return prisma.shopSettings.findMany();
}

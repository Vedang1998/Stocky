# NON-AUTHORITATIVE. Production isolation uses digest-pinned library images
# in lib/isolated-executor.js (postgres/redis/node @sha256). This Dockerfile
# is not built or executed by the runner. Do not substitute it for the pins.
